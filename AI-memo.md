# AIによる3次元探索支援の開発メモ

created: 2026-09-06 23:42 (JST)
update: 2026-09-07 00:35 (JST)
author: Codex (GPT-6 Astra)

## 1. 所要時間とトークン量

対象は2026-09-06の「実装してください。お願いします。」から初期版の完了報告まで。統計計算だけでなく、既存成果の読解、設計反映、コード実装、修正、検証、日本語考察、HTML表示確認、報告書作成を含む。これ以前の計画相談と、このメモ作成・Git操作は含めない。

- **経過時間：約239分（約4時間）**。セッションログの開始19:34:12から完了側の記録23:33:15（JST）まで。待機・ツール実行を含む壁時計時間であり、Rエンジン単体の処理時間ではない。
- **生成トークン：約46,874（約4.7万）**。ログの累積output_tokensの差分。reasoning_output_tokensの差分6,238は内訳として扱い、別途加算しない。
- **入力トークン：約5,412,902（約541万）**。繰り返し渡される会話履歴・コード・ツール出力を含む延べ処理量であり、固有の文章量ではない。
- 入力のうち **キャッシュ済み：約5,156,864（約516万）**、差し引きの非キャッシュ入力：約256,038（約25.6万）。
- **入力＋出力：約5,459,776（約546万）**。

フェルミ推定ではなく、ローカルのセッションログにある累積token_countの前後差による概算である。サービスの課金明細や利用枠の算定値そのものではなく、ログ集計の定義・境界に依存する。モデル名はこの作業で利用したAstraとして記録した。

## 2. 利用ゲージ

**5時間枠の65％を消費した。**

これはユーザー申告のゲージ値。239分という経過時間や、上記トークン量とは別の指標である。55％を時間に換算して「165分動いた」とは解釈しない。

## 3. Skillとして起動すればレポートを再現できるか

**可能。ただし、数値計算の再現とAIの文章の完全一致は区別する。**

| 対象 | 現状 |
| --- | --- |
| 数値計算 | 同一入力・設定・R環境・seedで再現可能。別runへの再実行でmodels・comparisons・cells・posterior・checksの一致を確認した |
| 日本語考察 | スキルのPass 2に従ってAIが結果JSONから再作成する。表現・着目点の完全一致は保証しない |
| HTML | 計算結果、考察、品質確認、数値根拠一覧が揃えばスキルのrendererで再生成できる |
| 今回のレポートの完全保存 | output配下はGit管理外。今回の考察・HTMLを一字一句保持するには、そのrunディレクトリを別途保存する必要がある |

このリポジトリを開いたAIに、例えば次のように依頼する。

> $vcd-bayesian-evidence-analysis を使い、examples/titanic.csvをClass × Sex × Survivedで分析してください。Ageは合算し、目的変数はSurvivedとします。Pass 0で前提を確認し、新しいrun IDで計算、日本語考察、品質確認、数値根拠一覧、HTMLまで作成してください。旧経路ではなく新しい3次元経路を使ってください。

[スキル本体](.agents/skills/vcd-bayesian-evidence-analysis/SKILL.md)が入口。[設定例](.agents/skills/vcd-bayesian-evidence-analysis/templates/three_way/config_example.json)を実際の検分ハッシュ・出力先に合わせて使用する。RのコマンドだけではAI考察は自動生成されないため、スキルを読むAIがPass 2/2.5を担当する。

新しい環境ではRと必要パッケージを用意する。生成済みoutputがGitに含まれなくても、Titanic元CSV、エンジン、設定例、検証用生成スクリプトはリポジトリにある。HairEyeColorはR標準データから生成できる。実行時刻やrun IDまで同一になることは求めない。

## 4. 今回の保存範囲と次の確認

ユーザーの明示指示により、このメモと現在の実装・仕様・検証コード・文書をコミットし、origin/mainへpushする。既存の未push計画コミット1件も送信対象。生成結果の強制追加、他worktreeの変更、OpenSpecのarchiveは行わない。

[実装・検証報告](docs/Artifacts/statistical_validation_001_0906.md)には98項目の検証結果と6レポートへのリンクがある。講座での最終利用者受入、実RWD1件での受入は未実施。次はレポートの説明・図表を確認してから、実データ1件で試す。

## 5. 4元以上への拡張に関する次期開発課題

3元経路で比較した9モデルは、全主効果を含む階層対数線形モデルを列挙した数である。4元になると、同じ条件での候補モデル数は36ではなく **114モデル（飽和モデルを含む）** になる。飽和モデルを除けば113モデルである。5元では同じ数え方で6,894モデルとなり、変数数を1つ増やすだけで全探索は急速に現実的でなくなる。

モデル数は主に変数数、相互作用の最大次数、階層性、候補モデルの制約で決まる。水準数はモデル構造の個数を変えないが、セル数を水準数の積だけ増やし、パラメータ数、疎セル、構造ゼロ、非収束、可視化の難しさに影響する。例えば水準数が4・3・2・2なら4元表は48セルである。

次期開発では、4元の114モデルをそのまま利用者向けに全列挙するのではなく、次の順で機能化する。

1. **候補モデル記述形式**：主効果、2因子・3因子・4因子相互作用を設定ファイルで指定する。
2. **階層性検証**：上位相互作用を指定したとき、必要な下位相互作用が自動的に含まれるか検査する。
3. **探索範囲の制御**：全探索、2因子まで、指定した3因子のみ、事前登録候補のみを選べるようにする。
4. **モデル探索の説明可能性**：BIC、尤度差、疎らさ、非収束、事前の根拠を記録し、単純な自動勝者決定を避ける。
5. **安定性評価**：候補モデルとセル順位を再標本化し、モデル選択頻度、上位K包含率、順位分布、適合成功率を報告する。
6. **4次元の表示設計**：単一の図に押し込めず、1変数を固定したスライス、層別ヒートマップ、条件付き割合の比較として表示する。

4元対応の最初の実装目標は、114モデルの完全自動比較ではなく、**候補モデルを安全に指定・検証し、計算量と統計的限界をレポートできる基盤**とする。3元経路の講座受入とセル順位安定性の検証を終えた後に、4元の候補モデル指定を別の実装計画として開始する。

### 水準数とセル数の具体例

4元表では、各変数の水準数を掛け合わせた値が最大セル数になる。モデル構造の候補数は同じでも、水準数が増えると1セルあたりの観測数が減り、疎らさや推定保留が起こりやすくなる。

| 水準数の例 | 最大セル数 | 総度数の例 | 1セル平均の目安 | 学習上の注意 |
| --- | ---: | ---: | ---: | --- |
| 2 × 2 × 2 × 2 | 16 | 1,600 | 100 | 小規模な教材に適する。4元交互作用を説明しやすい |
| 4 × 3 × 2 × 2 | 48 | 4,800 | 100 | Titanicのようなカテゴリを拡張した実務教材に近い |
| 5 × 4 × 3 × 2 | 120 | 6,000 | 50 | 層によって少数セルが生じやすく、ゼロセル確認が必要 |
| 10 × 5 × 4 × 3 | 600 | 6,000 | 10 | 平均値だけでは不十分。局所的な疎らさと非収束を重点確認 |
| 10 × 8 × 6 × 5 | 2,400 | 12,000 | 5 | 4元全探索の前に集約、層別、候補モデル制限を検討 |

例えば `4 × 3 × 2 × 2` の表は48セルなので、主効果を含む114モデルを比較する計算自体は可能である。一方、`10 × 8 × 6 × 5` では2,400セルとなり、各セルの平均度数が5程度に過ぎない。総度数が同じでも水準の細分化によって推定の安定性は大きく変わるため、Pass 0では総度数だけでなく、セル数、空セル数、最小度数、分位点、層別の偏りを確認する必要がある。

水準数を増やす場合の開発上の判定例は次のとおりである。

- **16セル程度**：9モデル・114モデルの構造比較を教材として実行しやすい。
- **50〜120セル程度**：候補モデル比較とセル診断を行えるが、疎セル保留を標準出力に含める。
- **600セル程度**：全モデル比較より、事前に指定した相互作用候補と層別表示を優先する。
- **2,000セル超**：変数の集約、層別解析、二段階探索、または階層ベイズモデルを検討し、全探索を既定動作にしない。
