# Titanic 3次元探索テストの分析スコープ

created: 2026-09-07 00:20 (JST)
update: 2026-09-07 00:20 (JST)
author: Codex (GPT-5)

## 目的

`examples/titanic.csv`を用いて、現行 `vcd-bayesian-evidence-analysis` の3次元経路を起動し、計算結果から日本語考察・品質確認・HTMLダッシュボードまで生成できることを確認する。これはスキルの表示・再現性テストであり、Titanicデータから因果効果を推定するものではない。

## Pass 0の検分結果

- 入力: `examples/titanic.csv`
- 入力行数: 32行、列数: 5列
- 欠測: なし
- 度数列: `Freq`
- 総度数: 2,201
- 水準: `Class` 4、`Sex` 2、`Age` 2、`Survived` 2
- 0度数セル: あり。構造ゼロとは確定せず、標本ゼロとして扱う。
- 入力SHA-256: `69af742ef405fc4ec8c0d485da43f837d8fdc54f13c5f4f2ab817873ba8b7ce1`

## 変数選択

3次元経路の上限に合わせ、`Class × Sex × Survived`を分析軸とする。`Age`は目的変数や層別軸に入れず、同じ3元セル内で合算する。これにより、前回検証と同じダッシュボード構造を再現し、`Survived`を目的変数として条件付き生存割合を表示する。

## 前提と保留

- 集計度数を個票へ展開しない。
- 標本単位は乗客・乗員と仮定するが、教材CSVだけから抽出設計や依存性を完全には検証できない。
- 独立性はテスト上 `assumed_independent` とする。実務データではPass 0で再確認する。
- 0度数は `sample_zero` とし、構造ゼロを指定しない。
- このテストでは、推定状態、スパースセル、図表の可読性、HTML生成の成否を確認する。
