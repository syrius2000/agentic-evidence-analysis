# Titanic 3次元探索テストの品質確認

## 計算状態

- `evidence_results.json`: `status=COMPUTED`
- schema: `three-way-results-v1`
- Pass 0検分、設定検証、Pass 1計算を完了
- Dirichlet事後の校正: JSONの`posterior.calibration.passed=true`
- 独立参照照合: このスキル単体の動作テストでは未実施。正式な統計検証は `tests/statistical_foundations/run_validation.R` を別途実行する。

## 数値と解釈

- 総度数2,201、16セル、欠測なしを確認した。
- M1、M8、M9の逸脱度とM1→M8、M8→M9の改善量は結果JSONから転記した。
- `ΔG²/N`を標準化効果量、局所改善をセルBF、全体log BFをセルの確信度として扱っていない。
- 条件付き生存割合の区間は点ごとの95%信用区間であり、多重性調整済みではない。
- M8→M9の近似推論は`HOLD`であり、P値やBIC近似log BFを断定していない。

## 可視化確認

- モデル比較表、入れ子比較、M1・M7・M8の層別ヒートマップ、全セル表、条件付き割合・区間、事前感度、考察、限界をHTMLに含める。
- ヒートマップの色は`log(O/E)`の符号と整合する。赤は期待より多く、青は期待より少ない。
- `leverage`を影響度や再標本化安定性として表示・解釈していない。
- ダッシュボードのHTML生成後に、ファイル存在、タイトル、主要見出し、JSONリンクを確認する。

## 残る限界

- 教材の集計表だけでは、元の抽出設計、独立性、構造ゼロの意味を完全には検証できない。
- セル順位のbootstrap安定性、多重性調整、選択後推論は未実装である。
- この結果はスキルの再現・表示テストであり、因果推論や実務上の意思決定を保証しない。
