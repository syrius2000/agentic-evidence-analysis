# 3次元統計基盤の検証・試作タスク

created: 2026-09-06 17:33 (JST)
update: 2026-09-06 17:33 (JST)
author: Codex (GPT-6)

[全体計画](../../../docs/Artifacts/implementation_plan_005_0906.md)と[設計](design.md)に基づく実装者向け一覧。チェックは実際の完了証拠を確認してから付ける。今回の計画作成で実装・分析計算を開始したものではない。

変更先は設計で列挙したtests/statistical_foundations/の6ファイル、tests/fixtures/statistical_foundations/の2ファイル、本tasksの進捗、および日本語検証報告に限る。派生入力・設定・実行結果は設計のrunディレクトリに保存する。本番スキル、共有schema、元CSVは変更しない。

## 2026-09-06 実装範囲の更新

ユーザーの明示的な実装指示に基づき、[統合実装計画](../../../docs/Artifacts/implementation_plan_006_0906.md)を今回の対象範囲の正本とする。下記の初期試作限定・本番変更対象外という記述は計画履歴であり、今回に限り同計画の正本エンジン・スキル統合・実例レポートまで拡張する。統計的な許容誤差と保留条件は維持する。

## 1. 実行範囲と検証契約の固定

- [x] 1.1 AGENTS.md、全体計画、proposal、specs、designを確認し、実装・試作に対する明示指示の対象範囲と開始時のgit status・git diffを記録する。完了確認: 既存差分と今回の変更予定が区別され、指示が未取得なら実装を開始していない。
- [x] 1.2 R本体、jsonlite、Pass 0に必要なdplyr・readrの利用可否とバージョンを確認する。完了確認: 環境情報が保存され、不足時の自動インストールや未承認の依存追加がない。
- [x] 1.3 case_manifest.jsonに9モデルの識別子・式・周辺制約、Titanicの2倍率と2探索モード、人工3表の生成式、水準順、乱数条件、誤差基準を固定する。完了確認: 設計との対応とJSONの読込を確認し、結果確認前の版を識別できる。
- [x] 1.4 3つのspecの全シナリオをmanifestに列挙し、実行検証・採否記録・後段の実LLM評価の担当箇所を対応付ける。完了確認: シナリオの漏れがなく、本変更で未実施となる後段評価が明示されている。

## 2. Pass 0・入力設定・出力隔離

- [x] 2.1 prepare_cases.Rに記述的な検分補足と3元集約を実装し、まず既存inspect_data.Rで元Titanicを検分する。完了確認: 元CSVの識別情報を保持し、Class × Sex × SurvivedへのAge合算で16セル・総度数2,201を確認する。モデル適合はまだ行わない。
- [x] 2.2 同ファイルでFreqだけ100倍の派生表と、設計の独立・AB関連・層別関連変化の人工表を生成する。各入力に既存検分と補足検分を行う。完了確認: 100倍表は16セル・総度数220,100で生の構成比が一致し、生成式・倍率・来歴を保存し、元入力を変更していない。
- [x] 2.3 試作用analysis_config.schema.jsonとvalidate_config.Rを実装する。完了確認: 異なる3変数、目的変数の所属、度数の非負整数・有限性・正の総和、水準、事前設定、未知キーを検証でき、無効入力を黙って修正しない。
- [x] 2.4 未記載セル・欠測・標本ゼロ・構造ゼロ、標本単位や独立性未解決時の拒否・保留を実装する。完了確認: 各異常ケースがmanifestの期待状態と理由に一致し、未解決の前提を持つモデル適合を開始しない。
- [x] 2.5 等値・集合包含だけの構造化抽出と、任意R式を受け付けない入力処理を実装する。完了確認: 正しい条件で対象度数が一致し、未知列・未知演算子・式文字列は拒否され、エラー時に全件解析へ切り替わらない。
- [x] 2.6 各入力の検分結果からanalysis_config.jsonを作成し、合意した設定を記録する。run_validation.Rの--config入口と出力先の衝突拒否を実装する。完了確認: モデル適合前にPass 0と設定確定の証拠が存在し、同じrun IDで既存結果を上書きしない。ここまでの条件を満たしてから次節へ進む。

## 3. 3元表モデルと独立参照計算

- [x] 3.1 fit_models.Rで9候補のPoisson GLMを適合し、モデル構造・適合値・対数尤度・階数・逸脱度・推定状態を返す。完了確認: manifestの9候補が過不足なく存在し、目的変数の指定だけでモデル集合が変わらない。
- [x] 3.2 reference_values.Rに独立・条件付き独立の閉形式、飽和の観測値、stats::loglinによる周辺制約からの参照計算を実装する。完了確認: 正則な人工表で閉形式と反復比例適合が一致し、参照処理がfit_models.Rの計算関数を呼んでいない。
- [x] 3.3 check_results.Rで主計算と参照適合値・逸脱度等を照合する。完了確認: 3人工表で事前の許容誤差を満たし、入れ子比較の自由度差と方向が記録され、生成モデルが常に選ばれることを合格条件にしていない。
- [x] 3.4 境界推定・識別不能・収束不良・近似条件不成立を検出し、通常の順位付けから分離する。完了確認: 該当ケースで値の欠如と理由を示し、推定失敗を隠した勝者判定がない。

## 4. 情報量規準とセルScoreの監査

- [x] 4.1 固定総度数の多項尤度とPoisson表現の比較関係、自由パラメータ数、標本サイズの導出を記録し、明示式のBICとstats::BICを照合する。完了確認: 絶対値とモデル間差を区別し、Nとセル数の違いが出力と採否記録に残る。
- [ ] 4.2 現行EBICの式・組合せ罰則・モデル空間を一次資料と照合する。完了確認: 各モデルの罰則を別々に確認し、9候補の探索における採用・不採用・保留の理由を記録する。根拠未確認の値を検証済みBFとしない。
- [x] 4.3 セルの観測・期待値、符号付き残差、観測/期待比、適用可能な割合差と、旧Scoreの再現値を実装する。完了確認: 基準モデル・分母・方向・評価総数・抽出規則が記録され、算出不能値を通常の順位に混ぜない。
- [x] 4.4 相互独立と均一連関を基準に、各セルのダミー追加モデルを再適合して尤度差・階数差を計算する。完了確認: 残差二乗との差を全対象セルで記録し、従属列・境界推定を保留し、局所BFとの同一視が正当化されるか採否記録へ渡す。

## 5. ベイズ計算の校正

- [x] 5.1 総集中度a=1と感度設定0.1・10によるDirichlet事前・事後を実装し、セル確率と条件付き割合の平均・95%等裾区間を求める。完了確認: 事前の意味・対象セル数・分母・区間の種類が記録され、解析値とベータ周辺分布の関係を独立に確認する。
- [x] 5.2 seed=20260906、20,000回の同時事後標本を生成し、解析平均と区間を設計のMCSE・累積確率基準で校正する。完了確認: 事前固定した許容条件を満たし、セル間・層間差に同一の同時標本を使い、点ごとの区間を同時区間と呼ばない。
- [x] 5.3 事後予測の不一致指標と集約方法を実行前にmanifestへ固定し、総度数を固定した予測表で適合確認と事前感度の比較を行う。完了確認: 指標の選択時点と結果を記録し、飽和モデルの予測確認を構造モデルの適合保証へ転用しない。
- [x] 5.4 独立モデルと飽和モデルの明示事前の下で周辺尤度・厳密log BFを解析計算し、近似値と比較する。完了確認: 多項係数の共通性と各事前の正規化を独立参照側で確認し、比較方向・事前感度・近似誤差を記録する。他の7モデルの厳密BFやモデル事後確率を計算済みとしない。

## 6. 統合した受入検証

- [x] 6.1 設計のprovenance・input_summary・models・comparisons・cells・posterior・checks・decisionsをJSONへ出力する。完了確認: 再読込でき、算出不能はnullと理由で表され、数値を丸めたゼロやInfを確実性と扱わず、計算状態と検証状態を区別する。
- [x] 6.2 単一のrun_validation.R --config入口でTitanicの元表・100倍版を、目的変数なし・Survived指定の双方で実行する。完了確認: 比較ごとの設定差が許容項目に限られ、独立参照値との照合結果・構成比の不変・標本サイズ感度を記録する。
- [x] 6.3 3人工表と異常ケースを同じ入口で検証する。完了確認: 全シナリオが証拠または後段評価へ対応し、失敗・保留・未実施を合格で埋めない。参照値を意図的に不一致にした確認では、照合処理が不合格を報告する。
- [x] 6.4 同じ環境・設定・乱数条件で新しい出力先へ再実行し、再現性と隔離を確認する。完了確認: 実行識別・時刻等を除く決定的な値が一致し、確率的推定が所定条件を満たし、過去結果・元CSVが保持される。既存スキル全体の受入や大量行の性能保証とは区別する。

## 7. 採否・後段への引継ぎ

- [x] 7.1 docs/Artifacts/statistical_validation_NNN_MMDD.mdに、候補モデル・BIC/EBIC・旧Score・セル診断・明示ベイズ計算の採否、根拠、適用範囲、計算負担、未検証事項を記録する。完了確認: 採用した方式には導出と数値検証の証拠があり、必要な証拠が不足する方式は採用済みになっていない。
- [x] 7.2 同報告へ階層的縮約の必要性・期待効果・計算基盤・説明負担を整理し、今回の校正計算との違いを示す。完了確認: 採用候補・後続課題・見送りの判断と理由があり、未実施の階層モデルを完成扱いしていない。
- [x] 7.3 同報告へ後段の本番実装・相談・レポートに渡す結果契約、互換性変更、実LLMの評価基準をまとめる。完了確認: 数値的主張の参照、全体とセルの区別、100倍化の説明、限界・次の分析の判定基準があり、実LLM評価と最終ユーザー受入は後段で未実施と明示する。
- [x] 7.4 検証報告の再現手順・環境・入力と出力の識別情報・仕様シナリオ対応を確認し、tasksの完了証拠と最終差分を照合する。完了確認: OpenSpecの厳格検証と文書リンク確認が通り、今回の変更範囲が設計内に収まり、Git管理外の結果の所在も引き継げる。commit・push・archiveは行わない。

## 8. 承認されたスキル統合

- [x] 8.1 正本エンジンと入力schema、Pass 0のハッシュ確認、構造化抽出を提供する。
- [x] 8.2 条件付き事後、M7追加基準、局所診断と非正則時の保留を実装する。
- [x] 8.3 日本語考察・品質確認を経由するHTML出力と3次元層別表示を提供する。
- [x] 8.4 Titanic・HairEyeColorの倍率比較を実行し、実際のAI考察と出力を確認する。
- [x] 8.5 利用案内・既存統計説明・今後の予定を整合させ、検証証拠と残課題を記録する。


## 完了証拠と残課題（今回の統合範囲）

[実装・検証報告](../../../docs/Artifacts/statistical_validation_001_0906.md)、[シナリオ対応](../../../tests/fixtures/statistical_foundations/case_manifest.json)を参照する。初期計画の配置は統合計画006に従って正本エンジンと独立参照へ分離した。98項目、9ケース、6レポートを確認した。

4.2のEBIC原典モデル空間の全面監査は未実施。新経路はEBIC不採用のため、今回の機能を阻害しない。5.3のPPCは主事前だけで実施し、事前感度は割合・差・BFで確認した。7.3の実LLM評価は今回の実例考察作成・数値照合へ前倒ししたが、別LLMの盲検評価と最終利用者受入は未実施。上記以外の未実装の階層・bootstrap・FREQ/MEANSを完了扱いしない。
