## 1. 実装前確認と作業分離

- [ ] 1.1 【実装担当／実装承認後】正本リポジトリの現在ブランチ、HEAD、追跡先、ahead/behind、既存差分、関連worktreeを確認し、開始時点の差分台帳と実行コマンドを実装報告へ記録する。
- [ ] 1.2 【実装担当／実装承認後】最新と確認した `origin/main` から専用worktreeと `codex/dashboard-unification` ブランチを作成し、本changeと `docs/Artifacts/implementation_plan_013_0912.md` を名前指定で移送してハッシュ一致を確認する。現行のdirty worktreeを変更していないことを再確認する。
- [ ] 1.3 【実装担当】Antigravity版OTCダッシュボードを変更前UI標準fixtureとして `tests/fixtures/dashboard_ui/` に保存し、取得元、commit、生成条件、SHA-256をmanifestへ記録する。fixtureが元ファイルを上書きしていないことを検証する。

## 2. Pass 0と設定契約

- [ ] 2.1 【実装担当】`examples/ucb_admissions.csv` にPass 0検査を実行し、`Dept`、`Gender`、`Admit`、度数列、欠測、ゼロセル、24完全セル、総度数4526を確認する。結果と採用理由を検査Artifactへ記録する。
- [ ] 2.2 【実装担当】Pass 0の合意内容から、A=`Dept`、B=`Gender`、C=`Admit`、応答変数=`Admit` を明示した `analysis_config.json` を生成し、後続処理がこの設定だけを参照することをテストする。
- [ ] 2.3 【実装担当】`base_models` と `conditional_rate_view` のschemaおよび入力検証を追加し、列名、分子、分母、比較軸、層別軸、信用水準、参照水準の正常系と異常系をテストする。
- [ ] 2.4 【実装担当】応答変数や水準指定が不明・矛盾する場合に暗黙補完せず、条件付き率セクションだけを `HOLD` にすることを固定例で検証する。

## 3. 三次元計算と結果JSON

- [ ] 3.1 【実装担当】Antigravity正本テンプレートを基礎に、全主効果を含むM1〜M9、因子記号対応、モデル式を選択的に統合する。既存 `templates/three_way` は互換経路として維持し、正本経路との境界をコードと文書で示す。
- [ ] 3.2 【実装担当】全モデルでポアソン対数尤度に基づく明示式 `BIC = -2 logL + p logN` を使用し、R既定BICや逸脱度置換を使用していないことをコード検査と回帰テストで証明する。
- [ ] 3.3 【実装担当】UCB固定fixtureでM5のBICが332.3119、M8が339.1982となり、M5が最良モデルとして選択されることを許容誤差つきテストで検証する。
- [ ] 3.4 【実装担当】M1と選択モデルのセル診断を別々のJSONグループへ出力し、各グループがEffect、Evidence、Influence、Stabilityの値、判定、正値セル数、全セル数を持つことをschemaとfixtureで検証する。
- [ ] 3.5 【実装担当】大標本Dual-Filterを `|log(O/E)| >= 0.50` かつ `T_score >= 3.84` の探索指標として実装し、単独で重要性・合否・因果を断定しない表示契約をテストする。
- [ ] 3.6 【実装担当】旧Evidence Scoreを監査専用列として保持し、真の信号判定、セル合否、セルBF、要約の主要結論に使われないことをコード検索と否定テストで確認する。
- [ ] 3.7 【実装担当】汎用 `conditional_rate_view` から条件付き率、Dirichlet事後平均、指定信用水準のETI、比較・層別ラベルを構造化JSONへ出力し、UCB以外の合成三次元データでも列名と水準を固定せず動作することをテストする。

## 4. エグゼクティブ要約と主張ゲート

- [ ] 4.1 【実装担当】要約を「結論」「根拠」「留保・次の確認」の3層で生成し、専門用語には画面内の短い説明を付ける。相対的モデル選択を因果・公平性・実務重要性へ拡張しないことをfixtureで検証する。
- [ ] 4.2 【実装担当】`narrative_claims.json` に入力結果ハッシュ、JSON Pointer、表示値、比較演算、生成文への参照を保存し、数値・参照・ハッシュが一致した主張だけをダッシュボードへ採用するゲートを実装する。
- [ ] 4.3 【実装担当】改変ハッシュ、存在しないpointer、丸め前後の不一致、HOLD対象を含む主張をゲートが拒否または保留することを失敗系テストで検証する。

## 5. デスクトップUIと完全オフライン化

- [ ] 5.1 【実装担当】因子凡例、最良モデル、エグゼクティブ要約、モデル比較、M1／選択モデル診断、条件付き率を、Antigravity版の視覚言語を保ったデスクトップ向けレイアウトへ統合する。
- [ ] 5.2 【実装担当】モデル比較表の初期順をBIC昇順とし、Model名、モデルクラス、自由度、逸脱度、BIC、ΔBICで並べ替えられることをブラウザテストで検証する。
- [ ] 5.3 【実装担当】M1と選択モデルを別タブまたは同等に明確な別表示とし、「正値／全セル」の分母と対象指標を隣接表示する。両モデルを合算した単一エビデンス率が存在しないことを確認する。
- [ ] 5.4 【実装担当】条件付き率を点・区間図と数値表で表示し、変数名・水準・分母・信用区間種別をデータ駆動で示す。無効設定時は当該セクションだけにHOLD理由を表示する。
- [ ] 5.5 【実装担当】最良モデル表示の「コンパクト帯」と「抑制したカード」の2案を同一データ・同一幅で撮影し、Ownerが選択した案と判断理由を計画または実装証跡へ記録する。選択されていない案を正本fixtureへ混在させない。
- [ ] 5.6 【実装担当】KaTeX相当の数式表現を `r-katex` 等で生成時に埋め込み、DataTablesの日本語文言、CSS、JavaScript、フォント方針をHTML内へ同梱する。外部CDN、Ajax、実行時ネットワーク要求がゼロであることを検証する。
- [ ] 5.7 【実装担当】資産の名称、版、ライセンス、取得・再生成方法を `THIRD_PARTY_NOTICES.md` と開発者向け文書へ記録する。実行時の自動インストールを追加せず、MacTeXをHTML表示の必須依存にしないことを確認する。

## 6. fixture生成と統合検証

- [ ] 6.1 【実装担当】Pass 0完了後に限り、UCBデータでPass 1〜3を専用出力先へ実行し、既存出力を上書きせず新しいUCB UI fixtureとmanifestを生成する。
- [ ] 6.2 【実装担当】Antigravity版OTC fixtureと新UCB fixtureの双方について、因子凡例、モデル式、BIC、セル診断、条件付き率、要約、HOLD表示の期待値を回帰テストへ固定する。
- [ ] 6.3 【実装担当】対象ユニット・schema・R回帰・render・ブラウザテストを実行し、コマンド、終了コード、主要件数、生成物ハッシュ、未検証事項を実装報告へ記録する。

## 7. 独立監査とOwner判定

- [ ] 7.1 【監査担当Codex】delta-specの全Scenarioを、コード位置、テスト、実行結果、画面証跡へ対応付ける監査表を作成する。`openspec status`、`valid: true`、チェック済みタスクだけを完了証拠にしない。
- [ ] 7.2 【監査担当Codex】明示式BIC、M5/M8固定値、4軸診断、Dual-Filter、旧Evidence Scoreの隔離、Dirichlet条件付き率を独立に再計算または追跡し、数理的一致と解釈境界を確認する。
- [ ] 7.3 【監査担当Codex】主張ゲートの成功・失敗、部分HOLD、汎用データセット、完全オフライン、デスクトップ幅、ソート操作を独立に再実行し、外部通信と絶対パス漏洩を検査する。
- [ ] 7.4 【監査担当Codex】開始時差分台帳と最終diffを照合し、既存のユーザー変更、旧OpenSpec change、Antigravity標準fixtureが保持されたことを確認する。申告外変更は未検証として報告する。
- [ ] 7.5 【Owner】実装・監査証跡と2案比較を確認し、UI採用、残存課題、main統合、commit、merge、pushをそれぞれ別に判断する。明示承認まではcommit、merge、pushを行わない。
