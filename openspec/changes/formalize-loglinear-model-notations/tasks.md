## 1. 事前準備とRエンジン: 比較基準固定・モデル仕様辞書・生成クラス正本化

- [ ] 1.1 変更前比較基準の固定: 変更前リビジョン（コミット `3956f22`）、比較入力データ（`data/raw/UCBAdmissions.csv` 等）、設定ファイル（`output/ucb_admissions/run_admit_bias/analysis_config.json`）、および既存結果（`run_admit_bias/evidence_results.json`）を特定・退避し、固定参照実行環境（対数尤度・未保存適合値の抽出スクリプト含む）を準備して記録する
- [ ] 1.2 `pass1_compute.R` に `generators` を正本とする辞書（3元表 M1〜M9、2元表 M1〜M2）を実装し、R スクリプト実行により辞書の構造と全キー（生成クラス、独立性メタデータ）の存在を検証する
- [ ] 1.3 `pass1_compute.R` に式生成関数（`build_formula_from_generators()`）と展開項一致検証関数（`validate_formula_terms()`）を実装し、全候補モデルで `terms()` による展開項集合と `generators` による期待項集合が `setequal` で完全一致することをユニットテストで検証する
- [ ] 1.4 `pass1_compute.R` に構造化独立性情報（`kind`, `statements`, `description_ja`）および LaTeX 数式文字列の導出処理を実装し、M8/M9 が条件付き独立記号を持たず交互作用制約として記録されることをテストで検証する
- [ ] 1.5 `pass1_compute.R` の `fit_all_poisson_models()` の戻り値拡張（`factor_map`, `definitions` 等）を行い、最終 JSON 構築・保存を担う `analysis.R` に反映して保存・再読込テストを実施し、`I()`（AsIs）により単一要素配列がスカラー化されずスキーマ定義に準拠することを検証する

## 2. ダッシュボード: 3段構成UIと数理注記・アクセシビリティ実装

- [ ] 2.1 `dashboard.Rmd` の最上部（主要指標カードの前）に「変数定義・因子記号凡例」ボックスを実装し、水準名と因子記号の違い注記とともに表示されることを HTML レンダリングで検証する
- [ ] 2.2 `dashboard.Rmd` の主要指標カードにおいて、有効な数理定義が存在する場合は BIC 最小モデルの構造仮定を表示し、定義未収録または不整合の場合はその状態を表示して推測補完しないロジックを実装・検証する
- [ ] 2.3 `dashboard.Rmd` のモデル比較表直前にブラケット記法の解説および BIC・ΔBIC の定義・比較方向の注記を配置し、比較表を 6 列構成（モデルと仮定、生成クラス、df_res、deviance、bic、Δbic）に更新して DT 内の生成クラスが等幅テキストとして正しく描画されることを確認する
- [ ] 2.4 `dashboard.Rmd` の DT 表直下に当該次元の候補モデル（3元表9候補、2元表2候補）の詳細アコーディオン（`<details>`）を配置し、有効な `best_model_id` のみが初期展開され、欠落・不正参照時に先頭候補が勝手に開かないこと、および狭い画面幅（レスポンシブ）でもレイアウトが崩れないことを確認する
- [ ] 2.5 表内の「数式・定義を見る」アンカーリンク操作時に該当 `<details>` を `open=true` にし、見出しへ `focus()` を移動させる JavaScript を実装し、キーボード操作の継続性をブラウザで検証する

## 3. フォールバック・異常系ハンドリングの実装

- [ ] 3.1 旧 JSON（`models.definitions` 未収録）の入力に対し、画面がクラッシュせず `input_summary.variables` から凡例を導出可能としつつ、詳細欄に「数学的定義未収録」と表示するフォールバック処理を実装・検証する
- [ ] 3.2 「数理定義はあるが適合結果・失敗理由が記録されていない」候補に対し「適合結果未収録」と表示する処理、および内部不整合データ（次元と変数の不一致、`best_model_id` 参照不正、実適合式と定義の不一致等）に対し数学的説明を保留して不整合状態を明示する処理を実装し、検証用フィクスチャ JSON で表示を検証する
- [ ] 3.3 記録済み適合失敗データ（ステータス失敗と理由が記録された検証用フィクスチャ JSON）に対し、保存された理由を表示して通常順位付けと分離する処理、および未知の `notation_version`（例: 2.0.0）に対する互換性警告と安全フォールバック処理を実装・検証する
- [ ] 3.4 外部数式ライブラリ（MathJax）の読み込み遅延・失敗時でも等幅テキストのブラケット表記と日本語構造解説によりモデル仮定が判読可能であること、およびアコーディオン展開後も MathJax 数式が乱れず正しく描画されることをブラウザ表示で検証する

## 4. 検証と数値不変性・回帰テスト

- [ ] 4.1 独立したモデル定義仕様（正解辞書）を用意し、R エンジンが生成する全 9 モデルの生成クラス・独立性記述が完全一致すること、および `vars` の並べ替えや `response_var` の指定有無に対して変数対応が一貫して追従することをテストで検証する
- [ ] 4.2 UCB Admissions データを用いて新規 run（別ディレクトリ）を実行し、モデルIDおよびセル水準をキーとして既存結果（`output/ucb_admissions/run_admit_bias/`）と結合比較する。保存値同士の比較では保存桁数内での完全一致を確認し、丸め前の内部計算値の比較では残差自由度が完全一致、BIC・逸脱度・セル診断値が許容誤差（絶対誤差または相対誤差 $< 10^{-6}$）内で一致することを検証スクリプトで確認する
- [ ] 4.3 事前準備（1.1）で固定した変更前エンジン参照実行の結果と比較し、モデルIDをキーとして対数尤度および未保存適合値が絶対誤差 $< 10^{-6}$ 内で一致することを検証する
- [ ] 4.4 2元表データセットを用いて分析を実行し、不要な第3因子記号 $C$ や 3元表定義（$[AB][C]$）が混入せず、2元表候補（$[A][B]$、$[AB]$）のみが正しく評価・表示されることを検証する
- [ ] 4.5 AI解釈文書更新と多層検証: (1) AI 向け指示文書（`SKILL.md`, `Reference.md`）を更新してモデル仮定と相対採択の峻別および差別不存在の非断定基準を明文化し、(2) `templates/pass2_stub.R` の出力が構造化定義と整合することをスタブ実行で検証し、(3) 既存の M7/M5 取り違え事例を含む少数の AI 考察サンプルを照合して、モデル定義と矛盾する断定や取り違えが適切に検出・抑制されることを確認する
