# QA0001.R6/R7 実施記録 001

created: 2026-09-24 14:15 (JST)
author: Auto (Composer)
plan: [implementation_plan_015_0924.md](implementation_plan_015_0924.md)
review: [s12_qa0001_cycle02_qa_001_0924.md](s12_qa0001_cycle02_qa_001_0924.md)

## 1. 対象

Cycle 2 受入後 hardening **QA0001.R6**（純R Draft-07 忠実度）と **QA0001.R7**（R表現正規化）。R8・Section 13 は未着手。

## 2. 対応内容

| ID | 結果 |
|---|---|
| R6 | `validate_payload_against_schema` に `minLength` / `uniqueItems` を追加。空文字・重複 `cluster_cols` / `matched_cols` の負例を追加。 |
| R6 実 Draft-07 | 一時 vendor `.tmp_jsonschema`（gitignore）上の Python `jsonschema` 4.26.0 で同 fixture を照合。local と Python が一致。 |
| R7 | design.md と `pass0_routing.R` コメントで length-1 character を native、正本 JSON は配列と明記。round-trip テスト追加。 |

## 3. 検証結果（実装者）

| 検証 | 結果 |
|---|---|
| `Rscript tests/test_comparative_schemas.R` | **136** assertions PASS / 0 FAIL（うち Python 一致 4件） |
| `Rscript tests/test_pass0_routing.R` | 40 PASS / 0 FAIL |
| `openspec validate comparative-evidence-reporting-v3 --strict --json` | valid |
| `git diff --check` | 問題なし |

### Validator 結果の分離記録

| Fixture | Local pure-R | Python Draft7Validator | expect |
|---|---|---|---|
| empty `cluster_cols` `""` | reject | reject | reject |
| duplicate `cluster_cols` | reject | reject | reject |
| duplicate `matched_cols` | reject | reject | reject |
| round-trip array `["custom_site"]` | accept | accept | accept |

独立 QA・Owner 判定・commit / push は未実施。
