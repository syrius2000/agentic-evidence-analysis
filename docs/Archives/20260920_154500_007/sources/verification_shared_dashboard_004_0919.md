# 共有ダッシュボード独立レビューF1〜F3の修正結果

created: 2026-09-19 11:01 (JST)
update: 2026-09-19 11:01 (JST)
author: Codex (GPT-6)

## 完了範囲

ユーザーの「君に実施を依頼したい」に基づき、[レビュー003](verification_shared_dashboard_003_0919.md) のF1〜F3を修正した。[計画008](implementation_plan_008_0919.md) と既存Changeのdesign・spec・tasksに範囲と受入条件を追記し、追加タスク5.1〜5.4を完了した。独立レビュー後の修正担当はCodexであり、本報告は実装者による検証記録である。

## 修正内容

| 指摘 | 修正 | 検証 |
| --- | --- | --- |
| F1 希少事象のゼロ化 | 条件付き割合・事後要約・群間差・感度差の固定小数丸めを除去。analysis.RのJSON出力もdigits=NAへ変更 | 実関数の主0.5・感度1.0をBeta解析式と比較し、本番書き出し式を評価したJSONを再読込して精度を確認 |
| F2 固定95%CI | 表列名を設定水準から生成しETI表記に統一。百分率の有効4桁表示とする。図の割合軸も有効桁表示 | 90%・95%の新規HTMLを生成し、列名・図タイトル・用語集・DataTablesに渡された非ゼロの数値を確認 |
| F3 冒頭の無条件N比例 | 「標本数が増えると、小さな差にも強い証拠が得られる場合があります」に変更 | 2D・3Dの共通用語集で旧表現の除去と本文の条件付きc倍説明の保持を確認 |

本番JSONの出力桁指定は結果全体に適用される。他指標の計算式や既存の明示丸めは変更していない。従来JSON既定値により追加で丸められていた数値の桁が増える場合があるため、新規生成結果のバイト列・ハッシュは旧結果と一致するとは限らない。既存runは更新していない。

表示表は数値を百分率へ変換し、列名に(%)を明示する。`DT::formatSignif(..., digits=4)` で有効桁整形し、固定小数桁による小さい正値のゼロ化を防ぐ。数値として渡すため並べ替えの数値型を維持する。

## F1再現ケースの修正後

度数(0,1,30000,40000,50000)、最初の2カテゴリを分子、主α=0.5、100,000ドロー、seed=77、90% ETI。修正前は3値すべて0だった。

| 指標 | 修正後のMC要約（表示用概数） | Beta解析値（概数） |
| --- | ---: | ---: |
| 事後平均 | 0.00001673 | 0.00001667 |
| ETI下限 | 0.000003017 | 0.000002961 |
| ETI上限 | 0.00003947 | 0.00003953 |

平均は6MCSE、分位点はBetaのCDF上で6標準誤差に基づく許容幅で照合した。ゼロ観測、小標本希少、大N希少、複数カテゴリ集約について実装関数とJSON往復を検証。群間差の区間、丸め前要約からの感度差、既存の全ゼロ分母HOLDも確認した。

## 実行結果

| コマンド | 結果 |
| --- | --- |
| `Rscript tests/test_shared_dashboard_math.R` | 8ブロック、99アサーション成功 |
| `Rscript tests/test_three_way_computation_engine.R` | 6ブロック、49アサーション成功 |
| `Rscript tests/test_three_way_dashboard_html.R` | 11ブロック、104アサーション成功 |
| `Rscript tests/test_vcd_categorical_dashboard_v4.R` | 15成功、0失敗 |
| `openspec validate shared-dashboard-theme-assets --strict --json` | valid=true、issues=[] |
| `git diff --check` | エラーなし |

追加数学テストの初回実行では、導入済みtestthatのexpect_lt/expect_gtがinfo引数を受け付けず停止した。不要な引数を除去した後、上記の全件が成功した。

3D検証では旧fixtureのband/cardに加え、新しい主事前で計算した希少ケースの90%・95%プレビューHTMLを一時ディレクトリに生成した。既存fixtureや封印runの上書きは行っていない。テストの既存後処理により一時出力は終了時に削除される。

## 証拠の限界と残る任意項目

生成HTMLの静的オフライン検査、HTML構造、DataTables設定・数値までは確認済み。前回ブラウザツールがfile URLをセキュリティ方針で拒否したため、今回も迂回を試みず、ブラウザ操作・実画面の視覚確認は未実施。全リポジトリテスト、全解析runの再計算は行っていない。

任意改善S1（3D感度比較表の追加）は今回対象外。新しい解析手法、規制提出用要件、テーマ追加、Pass 0既定値変更は行っていない。今回の修正についての別担当者による再レビューは未実施である。

## 作業ツリー境界

開始時HEADは61225bd。開始時から存在した未追跡のレビュー003、scratchロック2件とstaging考察1件を保持した。今回の変更は計算・出力・表示・用語集、関連3テスト、計画・Change・結果契約、本報告に限定する。commit・push・archiveおよび既存成果物の削除は行っていない。
