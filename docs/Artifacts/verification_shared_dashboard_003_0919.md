# 共有ダッシュボード・Jeffreys移行の独立レビュー

created: 2026-09-19 10:46 (JST)
update: 2026-09-19 10:46 (JST)
author: Codex (GPT-6)

## 判定と対象

対象は `shared-dashboard-theme-assets`、レビュー時HEADは `61225bd6153de0492ddcc8c0392f80850cefc88e`。大学統計講座と一般製薬社員の利用を想定する。共有資産と主事前移行の主要実装は確認できたが、大N・希少事象の結果保存に再現可能な問題があり、完成判定は修正後に再確認する。CRITICAL 1件、WARNING 2件、任意改善1件。ブラウザ操作は未検証であり、別途証拠を要する。

基準は [Change仕様](../../openspec/changes/shared-dashboard-theme-assets/specs/shared-dashboard-presentation/spec.md)、[design](../../openspec/changes/shared-dashboard-theme-assets/design.md)、[tasks](../../openspec/changes/shared-dashboard-theme-assets/tasks.md)、[計画008](implementation_plan_008_0919.md)。関連する正本の [条件付き割合](../../openspec/specs/conditional-rate-view/spec.md)、[3次元表示](../../openspec/specs/three-way-dashboard-reporting/spec.md)、[2次元分析](../../openspec/specs/two-way-evidence-analysis/spec.md) も参照した。

規制提出用システム認証、臨床意思決定システムとしての保証、新たな解析手法、モバイル対応、別テーマ追加は受入条件に追加しない。正本仕様の対象幅はデスクトップである。仕様上の要求と、教育・非専門家向けの任意改善を区別する。

## 完全性・正確性・整合性

| 軸 | 確認結果 |
| --- | --- |
| 完全性 | チェックボックス16/16。7要件すべてに実装箇所がある。ただしタスク4.4の実装対Beta照合・大N希少ケースの検証は完了根拠が不足し、独立試験で問題を再現した |
| 正確性 | 主0.5・感度1.0、同一支持集合、主事前メタデータ、レバレッジ、BIC定数差、集約Betaの説明を確認。希少確率の丸め、区間列名、冒頭説明に指摘あり |
| 整合性 | 共通CSS・辞書・用語集の利用、旧成果物の非再ラベル化を確認。同一事前で旧／新計算が一致。表示の一部に旧固定文言が残る |

## F1 — CRITICAL / P1：大N・希少事象の事後分布が保存時にゼロへ潰れる

対象: [pass1_compute.R](../../.agents/skills/vcd-bayesian-evidence-analysis/templates/pass1_compute.R) の592–596行と、その値から感度差を計算する箇所。

`post_mean`、`post_median`、`ci_lower`、`ci_upper` を小数4桁へ丸めて保存するため、正の小さい確率と区間が0になる。これはプロットの見た目だけではなくJSON値の情報損失である。

独立再現は1層・1比較群・5応答カテゴリ、度数 `c(0,1,30000,40000,50000)`、先頭2カテゴリを分子、5カテゴリ全部を分母、主α=0.5、100,000ドロー、seed=77、区間水準0.90を使用した。誘導事後は `Beta(2,120001.5)`。

| 指標 | Beta解析値 | 実装から返る値 |
| --- | ---: | ---: |
| 事後平均 | 約0.0000166662 | 0 |
| 90% ETI下限 | 約0.000002961 | 0 |
| 90% ETI上限 | 約0.00003953 | 0 |

有効状態は `VALID` のまま。講座や製薬社員向けに、非ゼロの推定をゼロ、有限の不確実性を幅ゼロと読み得る点が問題である。事前をJeffreysにすること自体の否定ではない。

根拠は計画008の「大Nかつ希少事象」「Beta解析式による平均・分位点照合」、タスク4.4、Changeの「複数カテゴリをまとめた確率を解釈する」シナリオ、正本の「事後平均・指定水準の事後信用区間を保存」。小数4桁丸めは旧実装からの継承だが、今回明示的に受け入れる範囲の問題なので指摘対象とする。

現在の [数学テスト](../../tests/test_shared_dashboard_math.R) 83–97行は `rbeta()` とBeta平均を比較するだけで、`compute_conditional_rate_view()` を呼んでいない。このテストだけでは実装の集約・丸め・分位点を検証できない。以前の状況報告で数学テストの網羅範囲を広く説明した点を訂正する。

最小修正は、結果保存と感度差計算に必要な精度を保持し、表示で適切に整形すること。JSON桁数契約への影響を同じChangeに明記する。テストは実関数の主／感度平均と区間を `qbeta()` 等へ直接照合し、ゼロ観測・希少・大N希少・全ゼロ層・複数カテゴリ集約を区別する。許容誤差はMC誤差と丸め誤差に基づくものとする。

## F2 — WARNING / P2：設定水準にかかわらず数値表が95%CIとなる

対象: [3D dashboard.Rmd](../../.agents/skills/vcd-bayesian-evidence-analysis/templates/dashboard.Rmd) 538–550行。

見出しと図タイトルは `config_echo.interval_level` を反映するが、表の列名と整形対象列は `95%CI下限`・`95%CI上限` に固定されている。例えば0.90設定では90% ETIの値に95%CIというラベルが付く。現行UCBの0.95設定では水準の誤表示は顕在化しない。

根拠はChangeの「共通用語集は次元と実際の解析条件に適合する」、design決定4の区間水準の一致、正本条件付き割合の指定水準契約。固定文字列は旧実装からの継承であり、新規回帰とは区別する。

最小修正は列名と整形対象列を実際の水準から生成し、`90% ETI下限` 等の信用区間であることが分かる表記にそろえること。0.90と0.95の生成HTMLを照合する。全面的な表デザイン変更は不要。

## F3 — WARNING / P2：用語集冒頭でEvidenceのN比例を無条件に一般化している

対象: [dashboard_glossary.R](../../.agents/shared/dashboard_glossary.R) 91行。

開いた直後の「3大利用原則」に「標本数比例の証拠強度と標本数不変の乖離倍率」とある。本文は同じ構成比でc倍して同じモデルを再適合するという条件を正しく述べるが、常時見える要約ではその条件が落ちている。一般の追加標本でもEvidenceがN比例すると受け取れる。

根拠はChangeの「レバレッジとスコアを確認する」シナリオ86行とdesign決定4。対象利用者が要約だけを読むことを想定すると、本文だけの訂正では十分でない。

最小修正案: 「効果の大きさと統計的証拠を分ける。標本数が増えると小さな差にも強い証拠が得られる場合がある」とし、厳密なc倍関係は条件を付した本文に置く。追加講義や新しい説明画面は不要。

## S1 — SUGGESTION：感度区間の比較へ辿れる小さな表を設ける

[3D dashboard.Rmd](../../.agents/skills/vcd-bayesian-evidence-analysis/templates/dashboard.Rmd) 459–466行は主／感度の平均・中央値の最大差だけを表示する。JSONには各層の主／感度区間と `eti_width_difference` があるが、表示では数値を一覧できない。

Changeは確率・区間の差を明示することを求めるが、その保存先を必ずHTMLとする規定は明確ではない。JSONには存在するため、これを独立した必須違反とは判定しない。利用者像に基づく任意改善として、折りたたみの小表で主／感度の平均・ETI・幅差を比較できるとよい。モデル誤りの自動判定や新しい閾値は導入しない。

## 再実行した検証

| 検証 | 独立実行結果 |
| --- | --- |
| OpenSpec strict validate | valid=true、issues=[]。構文証拠としてのみ扱う |
| 2D dashboard契約 | 15成功、0失敗 |
| 3D dashboard契約 | 10ブロック、74成功、0失敗。band/cardを新規生成 |
| 共有数学テスト | 7ブロック、33成功、0失敗。ただしF1のカバレッジ不足あり |
| 3D計算エンジン | 6ブロック、49成功、0失敗 |
| 旧／新実装の同一事前比較 | cee110fの旧関数と現関数をUCB、α=1、draws=5000、seed=42で比較。12割合の既存フィールドと6群間差が一致 |
| 独立Beta比較 | ゼロ分子、小標本希少、複数カテゴリ集約の平均・90%区間はMC誤差程度の差。大N希少でF1を再現 |
| 用語集HTML構造 | 現テンプレートから2D OTCと3D UCBを別の一時出力へ再生成。双方でsection-glossaryが1個、detailsが4個、用語集内pre/codeが0個 |

独立Beta比較の再現コード（生成コードやテストの修正は不要）:

```r
source('.agents/skills/vcd-bayesian-evidence-analysis/templates/pass1_compute.R')
d <- data.frame(A=paste0('a',1:5), B='b', C='c',
                Freq=c(0,1,30000,40000,50000))
s <- list(response_var='A', compare_by='B', stratify_by='C',
          numerator_levels=c('a1','a2'), denominator_levels=paste0('a',1:5),
          interval_level=0.90)
r <- compute_conditional_rate_view(d,c('A','B','C'),'Freq',s,
                                  draws=100000,seed=77)
r$rates[[1]]
2 / 120003.5
qbeta(c(.05,.95),2,120001.5)
```

## 検証限界と前回報告の整理

- ブラウザツールがローカルfile URLへのアクセスをセキュリティ方針で拒否した。回避は行っていない。今回確認したのは再生成・HTML構造・静的オフライン検査までであり、実ブラウザの開閉・列ソート・ネットワーク遮断・視覚的レイアウトは独立確認済みとはしない。既存報告002に3D previewの操作記録はあるが、それを今回の独立操作として数えない。
- 直接再生成時にはPandocの数式変換・div閉じ警告が出た。用語集の構造検査は通るが、警告から画面全体の無欠陥を断定しない。視覚確認が残る。
- 全テストスイート、全runの封印・ハッシュ再検証、全文献の書誌・原典照合は今回行っていない。正しい文献であるとの包括的保証をしていない。
- 前回挙げたPass 0ヘルパーの `prior_alpha=1.0` 既定値は今回の変更箇所ではなく、確認した本番呼び出しでは0.5が明示される。このChangeの完了阻害条件には追加しない。別保守の候補に留める。
- 過去の検証報告001・002は記録時点の状態として保存してよく、「当時未コミット」という過去記載の上書きは必須ではない。最新状態は本報告で区別する。実装担当者のAI名はGit内容だけから断定しない。

## 引継ぎと変更境界

Geminiへの修正範囲はF1〜F3と対応する回帰検証を同じChangeの残作業として扱うのが最小である。F1の数値保存精度は計画008・design・結果契約へ変更内容を明記する。新ChangeやCC-SDD移行を必要とする規模ではない。S1は任意の表示改善として分ける。

今回のリポジトリ追加物は本報告だけ。実装コード・テスト・spec・tasksは変更していない。開始時に存在したscratchのロック2件とstaging考察1件は保持した。commit・push・archive・削除は行っていない。
