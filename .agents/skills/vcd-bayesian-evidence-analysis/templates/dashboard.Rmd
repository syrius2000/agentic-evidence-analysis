---
title: "3次元エビデンス分析ダッシュボード"
date: "`r format(Sys.time(), '%Y年%m月%d日 %H:%M', tz='Asia/Tokyo')`"
output:
  html_document:
    theme: flatly
    highlight: tango
    toc: false
    self_contained: true
    mathjax: null
params:
  output_dir: "./skill_out/vcd_bayesian"
  run_dir: NULL
  preview_mode: false
  require_pass2: true
  layout_variant: "band"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)

`%||%` <- function(x, y) if (is.null(x)) y else x

find_agent_repo <- function() {
  d <- normalizePath(getwd(), winslash = "/", mustWork = FALSE)
  for (i in seq_len(25L)) {
    if (file.exists(file.path(d, ".agents", "shared", "run_scope.R"))) {
      return(d)
    }
    parent <- dirname(d)
    if (parent == d) {
      break
    }
    d <- parent
  }
  getwd()
}

repo_root <- find_agent_repo()
source(file.path(repo_root, ".agents", "shared", "dependency_check.R"))
source(file.path(repo_root, ".agents", "shared", "run_scope.R"))

check_r_dependencies(
  c("jsonlite", "DT", "dplyr", "htmltools", "htmlwidgets", "knitr", "rmarkdown", "katex", "ggplot2"),
  context = "vcd-bayesian-evidence-analysis ダッシュボード生成"
)

suppressPackageStartupMessages({
  library(jsonlite)
  library(DT)
  library(dplyr)
  library(htmltools)
  library(htmlwidgets)
  library(knitr)
  library(rmarkdown)
  library(katex)
  library(ggplot2)
})

# run_dir の直接受け入れと二重探索バイパス
output_dir <- if (!is.null(params$run_dir) && nzchar(trimws(params$run_dir))) {
  normalizePath(trimws(params$run_dir), winslash = "/", mustWork = TRUE)
} else {
  rs <- resolve_pass3_run_dir(params$output_dir, "evidence_results.json")
  rs$run_dir
}

layout_variant <- tolower(trimws(as.character(params$layout_variant %||% "band")))
if (!layout_variant %in% c("band", "card")) {
  layout_variant <- "band"
}

meta_file <- file.path(output_dir, "run_meta.json")
run_meta <- if (file.exists(meta_file)) {
  tryCatch(jsonlite::fromJSON(meta_file, simplifyVector = FALSE), error = function(e) NULL)
} else NULL

json_path <- file.path(output_dir, "evidence_results.json")
summ_path <- file.path(output_dir, "executive_summary.md")
claims_path <- file.path(output_dir, "narrative_claims.json")
qc_path <- file.path(output_dir, "quality_check.md")
preview_summ_path <- file.path(output_dir, "executive_summary_preview.md")

preview_mode <- isTRUE(params$preview_mode) || (!file.exists(summ_path) && file.exists(preview_summ_path))
require_pass2 <- isTRUE(params$require_pass2 %||% (!preview_mode))

if (!file.exists(json_path)) {
  stop(paste("evidence_results.json が見つかりません:", json_path))
}

if (require_pass2 && !file.exists(summ_path)) {
  stop(paste0(
    "executive_summary.md が見つかりません（Pass 2 未完了）: ", summ_path, "\n",
    "Pass 2 で生成してから Pass 3 を実行してください。\n",
    "プレビューのみ必要な場合は params$require_pass2 = FALSE または params$preview_mode = TRUE を指定してください。"
  ))
}

res <- fromJSON(json_path, simplifyVector = FALSE)

# --- 完全オフライン数式描画ヘルパー (KaTeX) ---
render_katex <- function(tex, display = TRUE) {
  if (is.null(tex) || !nzchar(trimws(tex))) return("")
  tryCatch({
    as.character(katex::katex_html(tex, displayMode = display, include_css = FALSE, preview = FALSE))
  }, error = function(e) {
    sprintf('<code class="tex-fallback">%s</code>', htmltools::htmlEscape(tex))
  })
}

# --- DataTables インライン日本語辞書（外部 ja.json CDN 参照ゼロ） ---
dt_ja_lang <- list(
  emptyTable = "テーブルにデータがありません",
  info = "_TOTAL_ 件中 _START_ 〜 _END_ 件を表示",
  infoEmpty = "0 件中 0 〜 0 件を表示",
  infoFiltered = "（全 _MAX_ 件から抽出）",
  lengthMenu = "_MENU_ 件表示",
  loadingRecords = "読み込み中...",
  processing = "処理中...",
  search = "検索:",
  zeroRecords = "一致するレコードが見つかりません",
  paginate = list(
    first = "先頭",
    last = "最終",
    `next` = "次へ",
    previous = "前へ"
  )
)

# メタ情報・基本変数の取得
dims_raw <- res$input_summary$variables %||% res$dimensions %||% res$core$dimensions
dims <- if (!is.null(dims_raw)) unlist(dims_raw) else character(0)
n_total <- as.numeric(res$input_summary$total_n %||% res$n_total %||% res$core$n_total %||% 0)
n_cells <- as.integer(res$input_summary$n_cells %||% res$n_cells %||% res$core$n_cells %||% 0)
log_n <- as.numeric(res$input_summary$log_n %||% res$log_n %||% res$core$log_n %||% 0)
response_var <- if (!is.null(res$input_summary$response_var %||% res$response_var)) as.character(res$input_summary$response_var %||% res$response_var) else NULL
large_sample_mode <- isTRUE(res$input_summary$large_sample_mode %||% res$large_sample_mode %||% res$core$large_sample_mode)

run_id_disp <- if (!is.null(res$provenance$run_id)) {
  as.character(res$provenance$run_id)
} else if (!is.null(res$run_id)) {
  as.character(res$run_id[[1]])
} else {
  ""
}

# モデル選択情報
best_model_raw <- res$models$best_model_id %||% res$model_selection$best_model
best_model_id <- if (!is.null(best_model_raw) && nzchar(trimws(as.character(best_model_raw)))) {
  as.character(best_model_raw)
} else {
  NULL
}

models_raw <- res$models$summary %||% res$model_selection$summary
models_summary <- if (is.data.frame(models_raw)) {
  models_raw
} else if (is.list(models_raw) && length(models_raw) > 0) {
  dplyr::bind_rows(models_raw)
} else {
  data.frame()
}

dimension <- res$models$dimension %||% length(dims)
raw_factor_map <- res$models$factor_map
definitions <- res$models$definitions

# 凡例マップの解決
has_factor_map_stored <- !is.null(raw_factor_map)
factor_map <- if (has_factor_map_stored) {
  raw_factor_map
} else {
  syms <- c("A", "B", "C")[seq_along(dims)]
  map <- list()
  for (i in seq_along(dims)) {
    map[[syms[i]]] <- list(symbol = syms[i], variable = dims[i], label = dims[i])
  }
  map
}

# best_model_def
best_model_def <- if (!is.null(definitions) && !is.null(best_model_id) && best_model_id %in% names(definitions)) {
  definitions[[best_model_id]]
} else {
  NULL
}

best_model_is_failed <- FALSE
if (!is.null(best_model_id) && nrow(models_summary) > 0 && "model_id" %in% names(models_summary)) {
  b_row <- models_summary[models_summary$model_id == best_model_id, , drop = FALSE]
  if (nrow(b_row) > 0) {
    st <- if ("status" %in% names(b_row) && !is.na(b_row$status[1])) as.character(b_row$status[1]) else ""
    err <- if ("error_message" %in% names(b_row) && !is.na(b_row$error_message[1])) as.character(b_row$error_message[1]) else ""
    bic_val <- if ("bic" %in% names(b_row) && !is.na(b_row$bic[1])) as.numeric(b_row$bic[1]) else NA_real_
    if (identical(st, "FAILED") || nzchar(err) || is.na(bic_val) || is.infinite(bic_val)) {
      best_model_is_failed <- TRUE
    }
  }
}

# 効果量情報
cramers_v <- suppressWarnings(as.numeric(res$effects$cramers_v %||% res$cramers_v))
cramers_v_ci <- res$effects$cramers_v_ci %||% c(res$effects$cramers_v_ci_low %||% res$cramers_v_ci_low, res$effects$cramers_v_ci_high %||% res$cramers_v_ci_high)
effect_status <- res$effects$effect_status %||% res$effect_status %||% "unknown"
effect_reason <- res$effects$effect_reason %||% res$effect_reason %||% ""

# セル診断（基準モデル別）データの展開
cells_by_base <- res$cells$by_base_model
if (is.null(cells_by_base) || length(cells_by_base) == 0L) {
  # 旧形式互換の単一基準ラップ
  single_base_id <- res$cells$base_model_id %||% "M1"
  cells_by_base <- list()
  cells_by_base[[single_base_id]] <- list(
    base_model_id = single_base_id,
    base_model_name = res$cells$base_model_name %||% "ベースラインモデル",
    question_ja = sprintf("基準モデル %s からの乖離の評価", single_base_id),
    counts = list(
      total_cells = n_cells,
      regular_cells = res$cells$regular_count %||% 0L,
      regular_rate = round((res$cells$regular_count %||% 0L) / max(1L, n_cells), 4),
      quarantined_cells = res$cells$quarantined_count %||% 0L,
      quarantined_rate = round((res$cells$quarantined_count %||% 0L) / max(1L, n_cells), 4),
      candidate_cells = sum(as.data.frame(res$cells$full_data)$is_candidate_dual_filter %||% FALSE, na.rm = TRUE),
      candidate_rate = round(sum(as.data.frame(res$cells$full_data)$is_candidate_dual_filter %||% FALSE, na.rm = TRUE) / max(1L, n_cells), 4)
    ),
    top_k_data = res$cells$top_k_data,
    full_data = res$cells$full_data
  )
}

base_model_keys <- names(cells_by_base)

# AI サマリーの読み込み
if (file.exists(summ_path)) {
  summary_text <- paste(readLines(summ_path, encoding = "UTF-8"), collapse = "\n")
} else if (file.exists(preview_summ_path)) {
  summary_text <- paste0(
    "> **【プレビュー】** 本考察はスタブ（executive_summary_preview.md）から読み込まれた暫定サマリーです。\n\n",
    paste(readLines(preview_summ_path, encoding = "UTF-8"), collapse = "\n")
  )
} else {
  summary_text <- "### AI エグゼクティブ・サマリー（未生成）\n\n> Pass 2 を実行してください。"
}

# 条件付き割合 (conditional_rate_view) データの取得
cond_rate_view <- res$conditional_rate_view
```

```{r embed-katex-css, results='asis'}
# KaTeX CSS をインライン埋め込み（完全オフライン）
css_file <- file.path(dirname(knitr::current_input(dir = TRUE)), "assets", "katex.min.css")
if (!file.exists(css_file)) {
  # 同一階層または repo 内の assets を探索
  cand_css <- file.path(find_agent_repo(), ".agents", "skills", "vcd-bayesian-evidence-analysis", "templates", "assets", "katex.min.css")
  if (file.exists(cand_css)) css_file <- cand_css
}
if (file.exists(css_file)) {
  cat("<style>\n")
  cat(paste(readLines(css_file, warn = FALSE, encoding = "UTF-8"), collapse = "\n"))
  cat("\n</style>\n")
}
```

```{css, echo=FALSE}
/* --- NEJM / Nature Medical 学術標準スタイル --- */
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Yu Gothic", "Meiryo", sans-serif;
  font-size: 14px;
  color: #152238;
  max-width: 1440px;
  margin: 0 auto;
  padding: 24px 32px;
  background-color: #f6f8fb;
  line-height: 1.6;
}

.dashboard-main-content {
  max-width: 1200px;
  margin: 0 auto;
  background: #ffffff;
  padding: 32px;
  border-radius: 4px;
  border: 1px solid #e2e8f0;
  box-shadow: none;
}

/* ヘッダーバナー (NEJM / 学術ネイビー調) */
.header-banner {
  background: #1f4d7a;
  color: #ffffff;
  padding: 22px 28px;
  border-radius: 4px;
  margin-bottom: 24px;
  box-shadow: none;
}

.header-banner h2 {
  margin: 0 0 8px 0;
  font-size: 21px;
  font-weight: 700;
  color: #ffffff;
  letter-spacing: -0.3px;
}

.header-banner .meta {
  font-size: 13px;
  opacity: 0.95;
  line-height: 1.6;
  color: #f1f5f9;
}

/* 因子記号・凡例ボックス (NEJM / 学術調) */
.factor-legend-box {
  background: #f6f8fb;
  border: 1px solid #e2e8f0;
  border-left: 4px solid #1f4d7a;
  border-radius: 4px;
  padding: 16px 20px;
  margin-bottom: 24px;
  box-shadow: none;
}
.factor-legend-box h4 {
  margin: 0 0 10px 0;
  font-size: 14px;
  font-weight: 700;
  color: #152238;
}
.factor-legend-list {
  display: flex;
  flex-wrap: wrap;
  gap: 16px 28px;
  margin: 0;
  padding: 0;
  list-style: none;
  font-size: 13.5px;
}
.factor-legend-list li {
  display: flex;
  align-items: center;
}
.factor-symbol-badge {
  display: inline-block;
  background: #1f4d7a;
  color: #ffffff;
  font-weight: 700;
  padding: 2px 9px;
  border-radius: 3px;
  margin-right: 8px;
  font-size: 12px;
}
.factor-role-badge {
  background: #e2e8f0;
  color: #1f4d7a;
  font-size: 11px;
  font-weight: 600;
  padding: 2px 6px;
  border-radius: 3px;
  margin-left: 6px;
}
.factor-note {
  margin-top: 10px;
  font-size: 12px;
  color: #64748b;
  line-height: 1.5;
}

/* 最良モデル表示：案A（横長コンパクト帯・NEJM調） */
.best-model-band {
  background: #f6f8fb;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 16px 20px;
  margin-bottom: 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 12px;
  box-shadow: none;
}
.best-model-band .badge-col {
  display: flex;
  align-items: center;
  gap: 12px;
}
.best-model-badge-large {
  background: #1f4d7a;
  color: #ffffff;
  font-size: 17px;
  font-weight: 800;
  padding: 6px 13px;
  border-radius: 4px;
  letter-spacing: 0.5px;
}
.best-model-band .desc-text {
  font-size: 14px;
  font-weight: 600;
  color: #152238;
}
.best-model-band .sub-text {
  font-size: 12px;
  color: #64748b;
}
.best-model-band .metrics-col {
  display: flex;
  align-items: center;
  gap: 16px;
  font-size: 13px;
  color: #334155;
  background: #ffffff;
  padding: 8px 14px;
  border-radius: 4px;
  border: 1px solid #e2e8f0;
}
.best-model-band .metrics-col strong {
  color: #152238;
  font-size: 13.5px;
}

/* 最良モデル表示：案B（抑制した独立カード・NEJM調） */
.best-model-card {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 20px 22px;
  margin-bottom: 24px;
  box-shadow: none;
}
.best-model-card .card-header-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid #e2e8f0;
  padding-bottom: 10px;
  margin-bottom: 12px;
}
.best-model-card .card-title {
  font-size: 15px;
  font-weight: 700;
  color: #152238;
}
.best-model-card .card-body-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 20px;
}
.best-model-card .model-title-large {
  font-size: 15px;
  font-weight: 700;
  color: #1f4d7a;
  margin-bottom: 6px;
}
.best-model-card .metrics-box {
  background: #f6f8fb;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 12px 14px;
  font-size: 12.5px;
}

/* カード型統計グリッド (NEJM調) */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12px;
  margin-bottom: 20px;
}
.stat-card {
  background: #f6f8fb;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 14px 12px;
  text-align: center;
  box-shadow: none;
}
.stat-card .value {
  font-size: 18px;
  font-weight: 700;
  color: #152238;
}
.stat-card .label {
  font-size: 11.5px;
  color: #64748b;
  margin-top: 4px;
  font-weight: 500;
}
.stat-card.regular-card .value { color: #0f766e; }
.stat-card.quarantine-card .value { color: #a16207; }
.stat-card.candidate-card .value { color: #6d28d9; }

/* セクション見出し (NEJM調) */
.section-title {
  font-size: 16px;
  font-weight: 700;
  color: #152238;
  border-left: 4px solid #1f4d7a;
  padding-left: 10px;
  margin: 28px 0 14px 0;
  letter-spacing: -0.2px;
}

/* AI サマリーボックス (NEJM調) */
.ai-summary-box {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 22px 24px;
  margin-bottom: 24px;
  line-height: 1.7;
  box-shadow: none;
}
.ai-summary-box h1, .ai-summary-box h2, .ai-summary-box h3, .ai-summary-box h4 {
  color: #152238;
  margin-top: 1.2em;
  margin-bottom: 0.5em;
  font-weight: 700;
}
.ai-summary-box h1 { font-size: 1.25em; border-bottom: 1px solid #cbd5e1; padding-bottom: 6px; }
.ai-summary-box h2 { font-size: 1.15em; border-bottom: 1px solid #e2e8f0; padding-bottom: 4px; }
.ai-summary-box h3 { font-size: 1.05em; }
.ai-summary-box table {
  width: 100%;
  margin: 14px 0;
  border-collapse: collapse;
  font-size: 13px;
}
.ai-summary-box th, .ai-summary-box td {
  border: 1px solid #cbd5e1;
  padding: 8px 10px;
  text-align: left;
}
.ai-summary-box th {
  background-color: #f1f5f9;
  font-weight: 600;
}

/* パネル */
.topk-panel {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  box-shadow: none;
  padding: 18px 20px;
  margin-bottom: 24px;
}

/* お知らせ・警告ボックス (NEJM調) */
.notice-box {
  border-radius: 4px;
  padding: 12px 16px;
  font-size: 13px;
  margin-bottom: 18px;
  line-height: 1.6;
}
.notice-warning {
  background: #fffbeb;
  border: 1px solid #fef3c7;
  border-left: 4px solid #d97706;
  color: #92400e;
}
.notice-info {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-left: 4px solid #1f4d7a;
  color: #152238;
}

/* タブ UI (NEJM調) */
.tab-container {
  margin-top: 16px;
}
.tab-buttons {
  display: flex;
  gap: 6px;
  border-bottom: 2px solid #e2e8f0;
  margin-bottom: 18px;
}
.tab-btn {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-bottom: none;
  padding: 9px 18px;
  font-size: 13.5px;
  font-weight: 600;
  color: #64748b;
  cursor: pointer;
  border-radius: 4px 4px 0 0;
  transition: all 0.15s ease;
}
.tab-btn:hover {
  background: #f1f5f9;
  color: #152238;
}
.tab-btn.active {
  background: #ffffff;
  color: #1f4d7a;
  border-color: #cbd5e1;
  border-top: 2px solid #1f4d7a;
  border-bottom: 2px solid #ffffff;
  margin-bottom: -2px;
}
.tab-pane {
  display: none;
}
.tab-pane.active {
  display: block;
}

/* アコーディオン・数式 */
.model-detail-accordion {
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  margin-bottom: 10px;
  background: #ffffff;
  overflow: hidden;
}
.model-detail-accordion summary {
  padding: 11px 15px;
  font-weight: 600;
  background: #f8fafc;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: space-between;
  outline: none;
}
.model-detail-accordion summary:hover {
  background: #f1f5f9;
}
.model-detail-content {
  padding: 14px 18px;
  border-top: 1px solid #e2e8f0;
  font-size: 13px;
  line-height: 1.6;
}
.formula-box {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 12px 14px;
  margin: 10px 0;
  overflow-x: auto;
}

/* 用語集・数理モデルアコーディオン (NEJM / Nature Medical 調 - px絶対指定による可読性保証) */
.glossary-accordion {
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  margin-bottom: 14px;
  overflow: hidden;
  transition: border-color 0.2s ease;
}
.glossary-accordion:hover {
  border-color: #1f4d7a;
}
.glossary-accordion summary {
  padding: 14px 18px;
  font-weight: 700;
  color: #152238;
  cursor: pointer;
  background: #f8fafc;
  user-select: none;
  font-size: 16px;
  outline: none;
}
.glossary-accordion summary:hover {
  background: #f1f5f9;
}
.glossary-accordion[open] summary {
  border-bottom: 1px solid #e2e8f0;
  background: #f1f5f9;
  color: #1f4d7a;
}
.glossary-body {
  padding: 20px 24px;
  font-size: 14.5px;
  color: #1e293b;
  line-height: 1.75;
}
.glossary-item {
  margin-bottom: 20px;
  padding-bottom: 16px;
  border-bottom: 1px dashed #e2e8f0;
}
.glossary-item:last-child {
  margin-bottom: 0;
  padding-bottom: 0;
  border-bottom: none;
}
.glossary-item-title {
  font-weight: 700;
  color: #1f4d7a;
  font-size: 16px;
  margin-bottom: 6px;
}
.glossary-def {
  color: #1e293b;
  font-size: 14.5px;
  line-height: 1.7;
  margin-bottom: 6px;
}
.glossary-meta {
  display: block;
  font-size: 13.5px;
  color: #475569;
  margin-top: 5px;
  line-height: 1.5;
}
.glossary-caution {
  display: block;
  font-size: 13.5px;
  color: #9b2945;
  margin-top: 5px;
  line-height: 1.5;
}
.glossary-body ol {
  padding-left: 22px;
  margin: 0;
}
.glossary-body li {
  margin-bottom: 10px;
  font-size: 14px;
  color: #1e293b;
  line-height: 1.65;
}

.dataTables_wrapper { font-size: 13px; }
```

```{r dashboard-main-open, results='asis'}
cat('<div class="dashboard-main-content">')
```

```{r header-banner, results='asis'}
cv_summary_str <- if (!is.null(cramers_v) && !is.na(cramers_v)) sprintf("%.4f", cramers_v) else "未算出"

cat(paste0(
  '<div class="header-banner">',
  "<h2>&#128202; 3次元エビデンス分析ダッシュボード</h2>",
  '<div class="meta">',
  "<strong>分析変数</strong>: ", paste(dims, collapse = " &times; "),
  if (!is.null(response_var)) paste0(" (応答変数: ", response_var, ")") else "",
  " &nbsp;|&nbsp; <strong>総度数</strong>: N = ", format(n_total, big.mark = ","),
  " &nbsp;|&nbsp; <strong>最良モデル(BIC)</strong>: ", htmltools::htmlEscape(best_model_id %||% "未選定"),
  " &nbsp;|&nbsp; <strong>全体効果量 (Cram&eacute;r's V)</strong>: ", cv_summary_str,
  if (nzchar(run_id_disp)) {
    paste0(" &nbsp;|&nbsp; <strong>run-id</strong>: ", htmltools::htmlEscape(run_id_disp))
  } else {
    ""
  },
  "</div></div>"
))

if (isTRUE(preview_mode)) {
  cat(paste0(
    '<div class="notice-box notice-warning">',
    '<h4 style="margin: 0 0 6px 0; color: #b45309; font-weight: bold;">&#9888; プレビュー版ダッシュボード (未封印)</h4>',
    '<div>本ダッシュボードはプレビュー出力（dashboard_preview.html）です。AI 考察（Pass 2）は確定していません。</div>',
    '</div>'
  ))
}
```

```{r factor-legend, results='asis'}
# 変数定義と因子記号の対応凡例ボックス
legend_items <- vapply(names(factor_map), function(sym) {
  item <- factor_map[[sym]]
  v_name <- as.character(item$variable %||% sym)
  v_label <- as.character(item$label %||% v_name)
  label_disp <- if (nzchar(v_label) && v_name != v_label) sprintf("%s（%s）", v_label, v_name) else v_name
  is_resp <- (!is.null(response_var) && identical(v_name, response_var))
  resp_badge <- if (is_resp) '<span class="factor-role-badge">応答変数</span>' else ""
  sprintf('<li><span class="factor-symbol-badge">%s</span> <strong>%s</strong>%s</li>',
          htmltools::htmlEscape(sym), htmltools::htmlEscape(label_disp), resp_badge)
}, character(1))

cat(paste0(
  '<div class="factor-legend-box">',
  '<h4>&#128273; 変数定義と因子記号の対応凡例</h4>',
  '<ul class="factor-legend-list">',
  paste(legend_items, collapse = "\n"),
  '</ul>',
  '<div class="factor-note">',
  '※ カテゴリの水準名と、分析上の因子記号（A, B, C）は異なります。以降のモデル構造や数式は本凡例の因子記号に基づきます。',
  '</div>',
  '</div>'
))
```

```{r best-model-view, results='asis'}
# 最良モデル表示：2案の分岐（layout_variant: "band" vs "card"）
best_model_desc <- if (!is.null(best_model_def)) {
  best_model_def$independence$description_ja %||% best_model_def$formula_description_ja %||% best_model_def$bracket_notation
} else {
  "モデル定義未収録"
}

best_bic_val <- NA_real_
runner_up_id <- "-"
runner_up_delta <- "-"
n_candidates <- nrow(models_summary)

if (nrow(models_summary) > 0 && "bic" %in% names(models_summary)) {
  sorted_m <- models_summary[order(as.numeric(models_summary$bic)), ]
  best_bic_val <- as.numeric(sorted_m$bic[1])
  if (nrow(sorted_m) >= 2L) {
    runner_up_id <- as.character(sorted_m$model_id[2])
    runner_up_delta <- sprintf("+%.2f", as.numeric(sorted_m$bic[2]) - best_bic_val)
  }
}

if (identical(layout_variant, "card")) {
  # 案B: 抑制した独立カード
  cat(paste0(
    '<div class="best-model-card">',
    '<div class="card-header-bar">',
    '<div class="card-title">&#127941; 最良対数線形モデル（明示式BIC最小）</div>',
    '<div style="font-size:12.5px; color:#64748b;">候補 ', n_candidates, ' モデル中の相対選択</div>',
    '</div>',
    '<div class="card-body-grid">',
    '<div>',
    '<div class="model-title-large">', htmltools::htmlEscape(best_model_id %||% "-"), ': ', htmltools::htmlEscape(best_model_def$bracket_notation %||% ""), '</div>',
    '<div style="font-size:14px; color:#1e293b; margin-bottom:8px;">', htmltools::htmlEscape(best_model_desc), '</div>',
    '<div style="font-size:12px; color:#64748b;">※ 相対評価の原則：本選択は候補モデル群における相対的採択であり、真の独立や因果関係、差の不存在を証明するものではありません。</div>',
    '</div>',
    '<div class="metrics-box">',
    '<div>明示式BIC: <strong>', if (is.na(best_bic_val)) "-" else sprintf("%.2f", best_bic_val), '</strong></div>',
    '<div style="margin-top:4px;">次点モデル: <strong>', runner_up_id, '</strong> (ΔBIC: ', runner_up_delta, ')</div>',
    '<div style="margin-top:4px; font-size:11.5px; color:#16a34a; font-weight:600;">適合状態: 正常収束</div>',
    '</div>',
    '</div>',
    '</div>'
  ))
} else {
  # 案A: 横長コンパクト要約帯 (既定)
  cat(paste0(
    '<div class="best-model-band">',
    '<div class="badge-col">',
    '<div class="best-model-badge-large">', htmltools::htmlEscape(best_model_id %||% "-"), '</div>',
    '<div>',
    '<div class="desc-text">', htmltools::htmlEscape(best_model_desc), ' (<code>', htmltools::htmlEscape(best_model_def$bracket_notation %||% ""), '</code>)</div>',
    '<div class="sub-text">候補 ', n_candidates, ' モデル中 BIC 最小 | ※ 相対的採択であり、真の独立や因果関係を証明するものではありません</div>',
    '</div>',
    '</div>',
    '<div class="metrics-col">',
    '<div>明示式BIC: <strong>', if (is.na(best_bic_val)) "-" else sprintf("%.2f", best_bic_val), '</strong></div>',
    '<div>次点 (', runner_up_id, '): <strong>ΔBIC ', runner_up_delta, '</strong></div>',
    '</div>',
    '</div>'
  ))
}
```

<div class="section-title">&#129504; AI エグゼクティブ・サマリー（Pass 2 専門的考察）</div>

```{r ai-summary, results='asis'}
cat('<div class="ai-summary-box">\n')
cat(knitr::knit(text = summary_text, quiet = TRUE))
cat("\n</div>\n")
```

```{r conditional-rate-section, results='asis'}
# 条件付き割合セクション (conditional_rate_view)
if (!is.null(cond_rate_view)) {
  st <- cond_rate_view$status %||% "UNKNOWN"
  cfg <- cond_rate_view$config %||% list()

  cat('<div class="section-title">&#128200; 条件付き割合と事後不確実性（Dirichlet推論）</div>')

  if (identical(st, "HOLD")) {
    reasons <- cond_rate_view$hold_reason %||% list(general = "条件付き割合の設定が無効または不足しています。")
    cat('<div class="notice-box notice-warning">')
    cat('<strong>&#9888; 条件付き割合の算出保留 (HOLD)</strong>:<br>')
    cat('<ul>')
    for (r in unlist(reasons)) {
      cat('<li>', htmltools::htmlEscape(r), '</li>')
    }
    cat('</ul>')
    cat('※ 本セクションは保留されましたが、他の対数線形モデル比較および局所セル診断は正常に完了しています。')
    cat('</div>')
  } else {
    resp_v <- cfg$response_var %||% "Response"
    strat_v <- cfg$stratify_by %||% "Stratum"
    comp_v <- cfg$compare_by %||% "Group"
    num_lvs <- paste(unlist(cfg$numerator_levels), collapse = ", ")
    denom_lvs <- paste(unlist(cfg$denominator_levels), collapse = ", ")
    int_pct <- round(as.numeric(cfg$interval_level %||% 0.95) * 100)

    cat('<div class="topk-panel">')
    cat(sprintf('<p style="margin:0 0 12px 0; font-size:13.5px; color:#334155;"><strong>注目割合</strong>: <code>P(%s = %s | %s, %s)</code> （分母: %s の和, %d%% 等裾信用区間 ETI）</p>',
                htmltools::htmlEscape(resp_v), htmltools::htmlEscape(num_lvs),
                htmltools::htmlEscape(strat_v), htmltools::htmlEscape(comp_v),
                htmltools::htmlEscape(denom_lvs), int_pct))
  }
}
```

```{r conditional-rate-plot, fig.width=9, fig.height=4.5, out.width="100%"}
if (!is.null(cond_rate_view) && identical(cond_rate_view$status %||% "", "VALID")) {
  rates_raw <- cond_rate_view$rates
  if (!is.null(rates_raw) && length(rates_raw) > 0L) {
    df_plot <- dplyr::bind_rows(lapply(names(rates_raw), function(nm) {
      item <- rates_raw[[nm]]
      data.frame(
        strat_level = as.character(item$strat_level),
        comp_level = as.character(item$comp_level),
        post_mean = as.numeric(item$post_mean),
        ci_lower = as.numeric(item$ci_lower),
        ci_upper = as.numeric(item$ci_upper),
        raw_rate = as.numeric(item$raw_rate),
        obs_num = as.integer(item$obs_numerator),
        obs_denom = as.integer(item$obs_denominator),
        stringsAsFactors = FALSE
      )
    }))

    # 比較変数のレベル順序
    df_plot$comp_level <- factor(df_plot$comp_level, levels = sort(unique(df_plot$comp_level)))
    df_plot$strat_level <- factor(df_plot$strat_level, levels = rev(sort(unique(df_plot$strat_level))))

    cfg <- cond_rate_view$config
    strat_label <- cfg$stratify_by %||% "層別軸"
    comp_label <- cfg$compare_by %||% "比較軸"
    resp_label <- sprintf("割合: %s (%s)", cfg$response_var %||% "Response", paste(unlist(cfg$numerator_levels), collapse = ","))

    font_family <- if (identical(as.character(Sys.info()["sysname"]), "Darwin")) "HiraginoSans-W3" else "sans"

    p <- ggplot(df_plot, aes(x = post_mean, y = strat_level, color = comp_level, group = comp_level)) +
      geom_point(position = position_dodge(width = 0.5), size = 3) +
      geom_errorbarh(aes(xmin = ci_lower, xmax = ci_upper), position = position_dodge(width = 0.5), height = 0.25, size = 0.8) +
      scale_color_manual(values = c("#1f4d7a", "#9b2945", "#2e7d7a", "#d97706", "#5b21b6", "#475569")) +
      scale_x_continuous(labels = scales::percent_format(accuracy = 1)) +
      labs(
        title = sprintf("層別条件付き割合の事後推論 (Dirichlet %s%% ETI)", round(as.numeric(cfg$interval_level %||% 0.95) * 100)),
        x = resp_label,
        y = strat_label,
        color = comp_label
      ) +
      theme_minimal(base_family = font_family, base_size = 12) +
      theme(
        panel.grid.minor = element_blank(),
        panel.border = element_rect(color = "#e2e8f0", fill = NA, size = 0.5),
        legend.position = "top",
        plot.title = element_text(face = "bold", size = 13, color = "#152238")
      )
    print(p)
  }
}
```

```{r conditional-rate-table}
if (!is.null(cond_rate_view) && identical(cond_rate_view$status %||% "", "VALID")) {
  rates_raw <- cond_rate_view$rates
  if (!is.null(rates_raw) && length(rates_raw) > 0L) {
    df_tbl <- dplyr::bind_rows(lapply(names(rates_raw), function(nm) {
      item <- rates_raw[[nm]]
      data.frame(
        "層別水準" = as.character(item$strat_level),
        "比較水準" = as.character(item$comp_level),
        "分子度数" = as.integer(item$obs_numerator),
        "分母度数" = as.integer(item$obs_denominator),
        "生割合" = as.numeric(item$raw_rate),
        "事後平均" = as.numeric(item$post_mean),
        "95%CI下限" = as.numeric(item$ci_lower),
        "95%CI上限" = as.numeric(item$ci_upper),
        check.names = FALSE,
        stringsAsFactors = FALSE
      )
    }))

    datatable(
      df_tbl,
      rownames = FALSE,
      options = list(dom = "t", paging = FALSE, language = dt_ja_lang)
    ) |>
      formatPercentage(c("生割合", "事後平均", "95%CI下限", "95%CI上限"), digits = 1)
  }
}
```

```{r conditional-rate-close, results='asis'}
if (!is.null(cond_rate_view) && !identical(cond_rate_view$status %||% "", "HOLD")) {
  cat('</div><!-- end topk-panel -->\n')
}
```

```{r model-comparison-section, results='asis'}
if (nrow(models_summary) > 0) {
  cat('<div class="section-title">&#9879; 対数線形モデル比較（総度数 N 基準の明示式BIC）</div>')
  cat('<div class="topk-panel" style="margin-bottom:24px;">')

  # KaTeX による数式表示
  bic_tex <- "\\mathrm{BIC} = -2 \\ln L + p \\ln N"
  cat('<div class="formula-box">')
  cat('<strong>総度数 N 基準のポアソン明示式BIC</strong>:<br>')
  cat(render_katex(bic_tex, display = TRUE))
  cat('<div style="font-size:12.5px; color:#475569; margin-top:6px;">')
  cat(sprintf('※ $L$: ポアソン完全尤度, $p$: 推定パラメータ数（切片含む）, $N$: 総度数（本解析: %s, $\\ln N = %.4f$）。<strong>値が小さいほど好ましい</strong>。<br>',
              format(n_total, big.mark = ","), log_n))
  cat('※ <strong>相対評価の原則</strong>: 最良モデルは候補集合における相対的採択であり、真の因果関係を証明するものではありません。')
  cat('</div></div>')
}
```

```{r model-table}
if (nrow(models_summary) > 0) {
  m_tab <- models_summary

  is_failed_row <- function(row) {
    st <- if ("status" %in% names(row) && !is.na(row$status[1])) as.character(row$status[1]) else ""
    err <- if ("error_message" %in% names(row) && !is.na(row$error_message[1])) as.character(row$error_message[1]) else ""
    bic_val <- if ("bic" %in% names(row) && !is.na(row$bic[1])) as.numeric(row$bic[1]) else NA_real_
    identical(st, "FAILED") || nzchar(err) || is.na(bic_val) || is.infinite(bic_val)
  }

  failed_flags <- vapply(seq_len(nrow(m_tab)), function(i) is_failed_row(m_tab[i, , drop = FALSE]), logical(1))
  valid_indices <- which(!failed_flags)
  valid_min_bic <- if (length(valid_indices) > 0 && "bic" %in% names(m_tab)) {
    min(as.numeric(m_tab$bic[valid_indices]), na.rm = TRUE)
  } else {
    NA_real_
  }

  m_tab$delta_bic <- vapply(seq_len(nrow(m_tab)), function(i) {
    if (failed_flags[i] || is.na(valid_min_bic) || !"bic" %in% names(m_tab)) {
      NA_real_
    } else {
      as.numeric(m_tab$bic[i]) - valid_min_bic
    }
  }, numeric(1))

  m_ids <- as.character(m_tab$model_id)

  model_assumption_col <- vapply(seq_along(m_ids), function(i) {
    mid <- m_ids[i]
    target_accordion_id <- sprintf("model-detail-%s", mid)
    def <- if (!is.null(definitions) && mid %in% names(definitions)) definitions[[mid]] else NULL
    short_desc <- if (!is.null(def)) {
      def$independence$description_ja %||% def$formula_description_ja %||% def$bracket_notation
    } else {
      as.character(m_tab$model_name[i] %||% mid)
    }
    sprintf('<strong>%s</strong>: %s <a href="#%s" class="model-detail-link" onclick="openModelDetail(\'%s\'); return false;" style="color:#2563eb; font-size:12px; margin-left:6px;">数式・定義</a>',
            htmltools::htmlEscape(mid), htmltools::htmlEscape(short_desc), target_accordion_id, target_accordion_id)
  }, character(1))

  generators_col <- vapply(seq_along(m_ids), function(i) {
    mid <- m_ids[i]
    def <- if (!is.null(definitions) && mid %in% names(definitions)) definitions[[mid]] else NULL
    if (!is.null(def)) {
      sprintf('<code>%s</code>', htmltools::htmlEscape(def$bracket_notation))
    } else {
      '<span style="color:#94a3b8;">-</span>'
    }
  }, character(1))

  df_res_col <- as.integer(m_tab$df_residual %||% m_tab$df)
  dev_col <- as.numeric(m_tab$deviance)
  bic_col <- as.numeric(m_tab$bic)
  dbic_col <- as.numeric(m_tab$delta_bic)

  table_6col <- data.frame(
    "モデルと仮定" = model_assumption_col,
    "生成クラス" = generators_col,
    "残差自由度" = df_res_col,
    "逸脱度" = dev_col,
    "BIC" = bic_col,
    "ΔBIC" = dbic_col,
    stringsAsFactors = FALSE,
    check.names = FALSE
  )

  best_row_idx <- if (!is.null(best_model_id) && !best_model_is_failed) which(m_ids == best_model_id) else integer(0)

  # DataTables 列ソートを有効化し、初期順を BIC 昇順（列インデックス 4）とする
  dt_models <- datatable(
    table_6col,
    escape = FALSE,
    filter = "none",
    rownames = FALSE,
    options = list(
      dom = "t",
      paging = FALSE,
      ordering = TRUE,
      order = list(list(4, "asc")),
      language = dt_ja_lang
    )
  ) |>
    formatRound(c("逸脱度", "BIC", "ΔBIC"), digits = 2)

  if (length(best_row_idx) == 1L) {
    dt_models <- dt_models |>
      formatStyle(
        "モデルと仮定",
        target = "row",
        backgroundColor = styleEqual(table_6col$`モデルと仮定`[best_row_idx], "#f0fdf4"),
        fontWeight = styleEqual(table_6col$`モデルと仮定`[best_row_idx], "bold")
      )
  }

  dt_models
}
```

```{r model-details, results='asis'}
# 各候補モデルの数理定義・構造仮定詳細（アコーディオン）
if (nrow(models_summary) > 0) {
  cat('<div style="margin-top:20px;">')
  cat('<h4 style="font-size:14px; font-weight:700; color:#0f172a; margin-bottom:12px;">&#128269; 各候補モデルの数理定義・構造仮定詳細</h4>')

  candidate_ids <- if (dimension == 3L) paste0("M", 1:9) else paste0("M", 1:2)
  should_open_id <- if (!best_model_is_failed && !is.null(best_model_id) && best_model_id %in% candidate_ids) best_model_id else ""

  for (mid in candidate_ids) {
    accordion_id <- sprintf("model-detail-%s", mid)
    heading_id <- sprintf("heading-%s", accordion_id)
    is_open <- (mid == should_open_id)
    open_attr <- if (is_open) " open" else ""

    sum_row <- if (mid %in% models_summary$model_id) models_summary[models_summary$model_id == mid, , drop = FALSE] else NULL
    def <- if (!is.null(definitions) && mid %in% names(definitions)) definitions[[mid]] else NULL

    status_badge <- if (!is.null(sum_row) && !best_model_is_failed && !is.null(best_model_id) && mid == best_model_id) {
      ' <span style="background:#15803d; color:white; font-size:11px; padding:2px 6px; border-radius:4px;">最良モデル (BIC最小)</span>'
    } else {
      ""
    }

    summary_title <- sprintf('%s: %s%s', htmltools::htmlEscape(mid), htmltools::htmlEscape(def$bracket_notation %||% "モデル"), status_badge)

    cat(sprintf('<details id="%s" class="model-detail-accordion"%s>', accordion_id, open_attr))
    cat(sprintf('<summary><span><strong>%s</strong></span><span style="font-size:12px; color:#64748b;">開閉</span></summary>', summary_title))
    cat('<div class="model-detail-content">')
    cat(sprintf('<h5 id="%s" tabindex="-1" style="outline:none; margin:0 0 8px 0; font-weight:700;">モデル %s の数理定義と構造仮定</h5>', heading_id, htmltools::htmlEscape(mid)))

    if (!is.null(def)) {
      cat('<p><strong>生成クラス（ブラケット記法）</strong>: <code>', htmltools::htmlEscape(def$bracket_notation), '</code>')
      if (!is.null(def$bracket_expanded)) {
        cat(' &nbsp;|&nbsp; <strong>実変数展開</strong>: <code>', htmltools::htmlEscape(def$bracket_expanded), '</code>')
      }
      cat('</p>')

      indep_disp <- def$independence$description_ja %||% def$formula_description_ja %||% "制約なし"
      cat('<p><strong>独立性仮定</strong>: ', htmltools::htmlEscape(indep_disp), '</p>')

      if (!is.null(def$formula_latex)) {
        cat('<div class="formula-box">')
        cat('<strong>ポアソン対数線形回帰式</strong>:<br>')
        cat(render_katex(def$formula_latex, display = TRUE))
        cat('</div>')
      }
      if (!is.null(def$fitted_formula)) {
        cat('<p style="font-size:12px; color:#475569;"><strong>適合 formula (R)</strong>: <code>', htmltools::htmlEscape(def$fitted_formula), '</code></p>')
      }
    }

    if (!is.null(sum_row) && nrow(sum_row) > 0 && !is.na(sum_row$bic[1])) {
      cat(sprintf('<div style="margin-top:8px; font-size:12.5px; color:#475569;">残差自由度: %s | 逸脱度: %.2f | BIC: %.2f | ΔBIC: %s</div>',
                  sum_row$df_residual[1] %||% "-",
                  as.numeric(sum_row$deviance[1] %||% 0),
                  as.numeric(sum_row$bic[1] %||% 0),
                  if (is.na(m_tab$delta_bic[which(m_ids == mid)[1]])) "-" else sprintf("%.2f", m_tab$delta_bic[which(m_ids == mid)[1]])))
    }

    cat('</div></details>')
  }
  cat('</div>')
}
```

```{r model-comparison-close, results='asis'}
if (nrow(models_summary) > 0) {
  cat('</div><!-- end topk-panel -->\n')
}
```

<div class="section-title">&#127919; 局所セル診断（基準モデル別残差・4軸診断フレームワーク）</div>

<p style="margin:0 0 16px 0; font-size:13.5px; color:#475569;">
セル診断は基準仮説に応じて異なる問いに答えます。<strong>M1基準</strong>（相互独立からの乖離）と<strong>M5基準</strong>（選択モデルからの残余乖離）をタブで切り替えて確認できます。<br>
※ 基準モデルごとに前提が異なるため、両者を合算した単一のエビデンス率は定義されません。
</p>

<div class="tab-container">
<div class="tab-buttons">
```{r tab-buttons, results='asis'}
for (k in seq_along(base_model_keys)) {
  bkey <- base_model_keys[k]
  active_cls <- if (k == 1L) " active" else ""
  label_text <- if (identical(bkey, "M1")) "M1基準（相互独立からの乖離）" else sprintf("%s基準（選択モデルからの残余乖離）", bkey)
  cat(sprintf('<button id="btn-tab-%s" class="tab-btn%s" onclick="switchCellTab(\'%s\')">%s</button>\n',
              bkey, active_cls, bkey, htmltools::htmlEscape(label_text)))
}
```
</div>

```{r cell-diagnostic-tabs, results='asis'}
col_rename <- c(
  "Observed" = "度数(O)",
  "Expected" = "期待値(E)",
  "Residual" = "ピアソン残差",
  "log_oe_ratio" = "効果比:log(O/E)",
  "scaled_diff" = "標準化差",
  "score_stat" = "Score統計量(T)",
  "p_value" = "P値",
  "log_p" = "対数P値",
  "leverage" = "Leverage(h)",
  "stability_status" = "診断状態"
)

for (k in seq_along(base_model_keys)) {
  bkey <- base_model_keys[k]
  active_cls <- if (k == 1L) " active" else ""
  b_diag <- cells_by_base[[bkey]]
  counts <- b_diag$counts %||% list()

  cat(sprintf('<div id="pane-tab-%s" class="tab-pane%s">\n', bkey, active_cls))
  cat('<div class="topk-panel">\n')

  cat(sprintf('<h4 style="margin:0 0 8px 0; font-size:15px; font-weight:700; color:#0f172a;">%s</h4>',
              htmltools::htmlEscape(b_diag$question_ja %||% sprintf("基準モデル %s による診断", bkey))))

  # 分母付き件数カード群
  n_tot_cell <- as.integer(counts$total_cells %||% n_cells)
  n_reg <- as.integer(counts$regular_cells %||% 0L)
  pct_reg <- round((counts$regular_rate %||% 0) * 100, 1)
  n_quar <- as.integer(counts$quarantined_cells %||% 0L)
  pct_quar <- round((counts$quarantined_rate %||% 0) * 100, 1)
  n_cand <- as.integer(counts$candidate_cells %||% 0L)
  pct_cand <- round((counts$candidate_rate %||% 0) * 100, 1)

  cat('<div class="stats-grid" style="margin:16px 0 20px 0;">')
  cat(sprintf('<div class="stat-card"><div class="value">%d / %d</div><div class="label">評価対象セル</div></div>', n_tot_cell, n_tot_cell))
  cat(sprintf('<div class="stat-card regular-card"><div class="value">%d / %d</div><div class="label">安定セル (%.1f%% REGULAR)</div></div>', n_reg, n_tot_cell, pct_reg))
  cat(sprintf('<div class="stat-card quarantine-card"><div class="value">%d / %d</div><div class="label">隔離セル (%.1f%% QUARANTINED)</div></div>', n_quar, n_tot_cell, pct_quar))
  cat(sprintf('<div class="stat-card candidate-card"><div class="value">%d / %d</div><div class="label">探索候補セル (%.1f%%)</div></div>', n_cand, n_tot_cell, pct_cand))
  cat(sprintf('<div class="stat-card"><div class="value">N = %s</div><div class="label">総観測度数</div></div>', format(n_total, big.mark = ",")))
  cat('</div>')

  cat(paste0(
    '<div class="notice-box notice-info" style="font-size:12.5px; margin-bottom:16px;">',
    '<strong>大標本 Dual-Filter 探索基準</strong>: <code>|log(O/E)| &ge; 0.50</code> かつ <code>Score統計量 T &ge; 3.84</code> かつ <code>REGULAR</code>。<br>',
    '※ 探索候補件数は実務的重要性やセル合否の自動断定ではなく、多重性を考慮した重点探索の対象を示します。',
    '</div>'
  ))

  # Top-K セル表
  to_df <- function(x) {
    if (is.data.frame(x)) x
    else if (is.list(x) && length(x) > 0) dplyr::bind_rows(x)
    else data.frame()
  }
  tk_data <- to_df(b_diag$top_k_data)
  if (nrow(tk_data) > 0L) {
    cat(sprintf('<div style="font-weight:700; font-size:14px; margin-bottom:8px; color:#0f172a;">&#127942; 効果比 |log(O/E)| 上位 %d セル</div>', nrow(tk_data)))

    disp_cols <- c(dims, intersect(names(col_rename), names(tk_data)))
    tk_disp <- tk_data[, disp_cols, drop = FALSE]
    for (orig in names(col_rename)) {
      if (orig %in% names(tk_disp)) names(tk_disp)[names(tk_disp) == orig] <- col_rename[[orig]]
    }
    for (v in dims) {
      if (v %in% names(tk_disp)) tk_disp[[v]] <- factor(tk_disp[[v]])
    }

    round_cols <- intersect(c("期待値(E)", "ピアソン残差", "効果比:log(O/E)", "標準化差", "Score統計量(T)", "対数P値", "Leverage(h)"), names(tk_disp))

    dt_tk <- datatable(
      tk_disp,
      filter = "none",
      rownames = FALSE,
      options = list(dom = "t", paging = FALSE, scrollX = TRUE, language = dt_ja_lang)
    ) |>
      formatRound(round_cols, digits = 4) |>
      formatSignif(intersect(c("P値"), names(tk_disp)), digits = 3) |>
      formatStyle("効果比:log(O/E)", backgroundColor = styleInterval(0, c("#fee2e2", "#dbeafe")), fontWeight = "bold") |>
      formatStyle("診断状態", backgroundColor = styleEqual(c("REGULAR", "QUARANTINED"), c("#dcfce7", "#ffedd5")),
                  color = styleEqual(c("REGULAR", "QUARANTINED"), c("#15803d", "#c2410c")), fontWeight = "bold")

    # HTML 出力用タグ
    cat(htmltools::renderTags(dt_tk)$html)
  }

  # 全セル展開 (<details>)
  full_data_b <- to_df(b_diag$full_data)
  if (nrow(full_data_b) > 0L) {
    cat('<details style="margin-top:16px;">')
    cat(sprintf('<summary style="cursor:pointer; font-weight:700; color:#2563eb; font-size:13.5px;">&#128203; 全 %d セル 4軸診断テーブル（クリックで展開）</summary>', nrow(full_data_b)))
    cat('<div style="margin-top:12px;">')

    disp_cols_f <- c(dims, intersect(names(col_rename), names(full_data_b)))
    full_disp <- full_data_b[, disp_cols_f, drop = FALSE]
    for (orig in names(col_rename)) {
      if (orig %in% names(full_disp)) names(full_disp)[names(full_disp) == orig] <- col_rename[[orig]]
    }
    for (v in dims) {
      if (v %in% names(full_disp)) full_disp[[v]] <- factor(full_disp[[v]])
    }
    round_cols_f <- intersect(c("期待値(E)", "ピアソン残差", "効果比:log(O/E)", "標準化差", "Score統計量(T)", "対数P値", "Leverage(h)"), names(full_disp))

    eff_col_idx <- which(names(full_disp) == "効果比:log(O/E)") - 1L
    if (length(eff_col_idx) == 0L) eff_col_idx <- 0L

    dt_full <- datatable(
      full_disp,
      filter = "top",
      rownames = FALSE,
      options = list(
        pageLength = 20,
        scrollX = TRUE,
        order = list(list(eff_col_idx, "desc")),
        language = dt_ja_lang
      )
    ) |>
      formatRound(round_cols_f, digits = 4) |>
      formatSignif(intersect(c("P値"), names(full_disp)), digits = 3) |>
      formatStyle("効果比:log(O/E)",
                  backgroundColor = styleInterval(0, c("#fff7ed", "#f0fdf4")),
                  color = styleInterval(0, c("#b45309", "#0f766e")),
                  fontWeight = "bold") |>
      formatStyle("診断状態",
                  backgroundColor = styleEqual(c("REGULAR", "QUARANTINED"), c("#f0fdf4", "#fef3c7")),
                  color = styleEqual(c("REGULAR", "QUARANTINED"), c("#0f766e", "#a16207")),
                  fontWeight = "bold")

    cat(htmltools::renderTags(dt_full)$html)
    cat('</div></details>')
  }

  # 旧指標監査 (<details>)
  leg_pos <- as.integer(counts$legacy_positive_cells %||% sum(full_data_b$evidence_score_legacy > 0, na.rm = TRUE))
  pct_leg <- round(leg_pos / max(1L, n_tot_cell) * 100, 1)

  cat('<details style="margin-top:16px;">')
  cat(sprintf('<summary style="cursor:pointer; font-size:13px; color:#64748b;">&#128203; 旧指標監査（Evidence Score = r&sup2; &minus; k&middot;log N）: 正値 %d / %d セル (%.1f%%)</summary>',
              leg_pos, n_tot_cell, pct_leg))
  cat('<div class="formula-box" style="font-size:12.5px; margin-top:8px;">')
  cat('<strong>旧 Evidence Score 監査定義</strong>:<br>')
  cat(render_katex("\\mathrm{Evidence\\;Score}_{ijk} = r_{ijk}^2 - k \\cdot \\ln(N)", display = TRUE))
  cat(sprintf('・基準モデル: <code>%s</code> | ペナルティ係数: $k = 1.0$, $\\ln N = %.4f$ (閾値: %.2f)<br>', bkey, log_n, log_n))
  cat('・※ 本指標は大標本下（N > 2,000）で過敏に飽和する性質があるため、<strong>監査専用列として隔離</strong>されています。真の信号判定・セル合否判定・セルBFには使用していません。')
  cat('</div></details>')

  cat('</div><!-- end topk-panel -->\n')
  cat('</div><!-- end tab-pane -->\n')
}
```
</div><!-- end tab-container -->

```{r glossary-prep, results='asis'}
k_in <- function(tex) render_katex(tex, display = FALSE)
k_disp <- function(tex) render_katex(tex, display = TRUE)
score_katex_display <- k_disp("T_i^{\\mathrm{score}} = \\frac{r_{P,i}^2}{1 - h_{ii}}")
bic_katex_display <- k_disp("\\mathrm{BIC} = G^2 - \\mathrm{df} \\cdot \\ln(N) = -2 \\ln L + p \\ln(N)")
dirichlet_katex_display <- k_disp("\\boldsymbol{\\pi} \\mid \\boldsymbol{y} \\sim \\mathrm{Dirichlet}(\\boldsymbol{y} + \\boldsymbol{\\alpha}), \\quad \\alpha_k = 0.5")
```

```{=html}
<div class="section-card" id="section-glossary" style="margin-top: 32px;">
  <div class="section-title">&#128218; 統計用語集・方法論解説・学術リファレンス (Glossary & Scientific References)</div>
  <div style="background:#f8fafc; border-left:4px solid #1f4d7a; padding:14px 18px; border-radius:4px; margin-bottom:18px;">
    <div style="font-weight:700; color:#152238; font-size:15px; margin-bottom:4px;">統計的・科学的問い:</div>
    <div style="color:#1e293b; font-size:14px; line-height:1.65;">本ダッシュボードで使用されている3次元対数線形モデル、4軸セル診断、Raoスコア検定、およびベイズDirichlet推論の定義・解釈境界および学術的出典は何か？</div>
    <div style="margin-top:10px; font-size:13.5px; color:#475569; border-top:1px dashed #e2e8f0; padding-top:8px; line-height:1.6;">
      <strong>3大利用原則:</strong> ① P値単独判定の排除（全体検定の有意差のみで実務的重要性や個別セルを判定しない） / ② 局所効果（Effect）と統計的証拠（Evidence）の厳格分離（標本数比例の証拠強度と標本数不変の乖離倍率を混同しない） / ③ 不確実性の明示（点推定ではなく 95% ETI 信用区間幅で精度を評価）
    </div>
  </div>
  <p style="font-size:14px; color:#475569; margin-bottom:18px;">
    ※ 各項目をクリックして開閉できます。臨床・疫学およびリアルワールドデータ分析におけるエビデンス解釈の拠り所としてご活用ください。
  </p>

  <!-- Accordion 1: 4軸セル診断 -->
  <details class="glossary-accordion" open>
    <summary><span>1.</span> 局所セル診断と 4 軸フレームワーク (Four-Axis Local Cell Diagnostics)</summary>
    <div class="glossary-body">
      <div class="glossary-item">
        <div class="glossary-item-title">【第1軸】Effect (局所対数効果比: log(O/E))</div>
        <div class="glossary-def">基準対数線形モデルの期待度数 E に対する観測度数 O の局所的乗法乖離 &lambda; = ln(O/E)。総度数 N の増加によってインフレしない標本規模不変尺度です。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 局所セル診断表（効果比:log(O/E) 列、正の乖離: 緑背景、負の乖離: 橙背景）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 因果効果そのものを表すものではありません。観測度数 O = 0 のセルは対数変換が不能なため -Inf として隔離（QUARANTINED）されます。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">【第2軸】Evidence (Leverage補正Score統計量 T_score / 対数P値)</div>
        <div class="glossary-def">ポアソンGLMにおけるハット行列対角成分（Leverage h_ii）でピアソン残差の局所分散を補正した Rao のスコア検定統計量：
```

```{r, results='asis', echo=FALSE}
cat(score_katex_display)
```

```{=html}
        特定セルにのみ局所特異母数 &delta;_i を追加したモデルに対する厳密な検定統計量であり、漸近的に自由度 1 のカイ二乗分布 &chi;&sup2;(1) に従います。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 局所セル診断表（Score統計量(T) 列、対数P値 列）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 標本サイズ N に比例して統計量が増大するため、大標本下では微小な差異でも極大化します。実務的・臨床的有意性を単独で証明するものではありません。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">【第3軸】Influence (レバレッジ h_ii)</div>
        <div class="glossary-def">ポアソン対数線形モデルのハット行列の対角成分 h_ii &isin; [0, 1]。当該セルが分割表全体のパラメータ推定に与える幾何学的影響度・てこ比を表します。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 局所セル診断表（Leverage(h) 列）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> レバレッジが高いセルは「モデル推定への影響力が大きい」ことを意味する幾何学的性質であり、データ異常や外れ値と同一視してはなりません。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">【第4軸】Stability (隔離条件 QUARANTINED)</div>
        <div class="glossary-def">以下の3条件のいずれかに該当する数値的不安定セル：① 観測度数 O = 0、② 期待度数 E &lt; 5.0、③ 高レバレッジ h_ii &ge; 0.80。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 局所セル診断表（診断状態 列: 琥珀色 #A16207 バッジ）および統計カード（隔離セル件数）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 漸近正規近似の破綻を防ぐための安全弁（隔離）であり、第三の臨床群や即座に削除すべき不正データではありません。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">大標本 Dual-Filter 原則</div>
        <div class="glossary-def">総度数 N &gt; 2,000 の大標本下で些細な統計的有意差に惑わされないための二段階選定基準：第1フィルタ（Effect軸: |log(O/E)| &ge; 0.50）&times; 第2フィルタ（Evidence軸: Score統計量 T &ge; 3.84, P &lt; 0.05）かつ REGULAR。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 局所セル診断表の探索候補セル件数</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 機械的な合否判定ではなく、多重比較下で優先的に精査すべき局所シグナルを抽出するスクリーニング基準です。</span>
      </div>
    </div>
  </details>

  <!-- Accordion 2: 対数線形モデル -->
  <details class="glossary-accordion">
    <summary><span>2.</span> 3次元対数線形モデル群と明示式 BIC (Hierarchical Log-Linear Models & Explicit BIC)</summary>
    <div class="glossary-body">
      <div class="glossary-item">
        <div class="glossary-item-title">階層的対数線形モデル群 (M1〜M9)</div>
        <div class="glossary-def">3つの因子 A, B, C の度数データに対して定義される階層構造モデル：
        <ul style="margin:6px 0; padding-left:20px;">
          <li><code>M1: [A][B][C]</code> 完全相互独立モデル</li>
          <li><code>M2: [AB][C]</code> / <code>M3: [AC][B]</code> / <code>M4: [BC][A]</code> 1対2の複合独立モデル</li>
          <li><code>M5: [AB][AC]</code> / <code>M6: [AB][BC]</code> / <code>M7: [AC][BC]</code> 条件付き独立モデル（M5は A与件下で BとCが独立）</li>
          <li><code>M8: [AB][BC][AC]</code> 全2因子交互作用モデル（3因子均一連関）</li>
          <li><code>M9: [ABC]</code> 飽和モデル（3因子交互作用を含む完全適合）</li>
        </ul>
        </div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 対数線形モデル比較表（モデルと仮定、生成クラス）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 対数線形モデルは変数間の対称な連関構造をモデル化するものであり、単独で有向な因果関係を主張するものではありません。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">総度数 N 基準の明示式 BIC</div>
        <div class="glossary-def">ポアソン完全尤度に基づく Schwarz の情報量規準。総標本サイズ N（表全体の総観測度数）をペナルティ項に明示的に採用：
```

```{r, results='asis', echo=FALSE}
cat(bic_katex_display)
```

```{=html}
        ここで G&sup2; はポアソン逸脱度（2 &sum; O ln(O/E)）、p はパラメータ数、df は残差自由度です。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 対数線形モデル比較表（BIC 列、&Delta;BIC 列）および最良モデル要約帯</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> セル数 K ではなく総観測度数 N を基準とすることで、大標本下での過剰適合（過剰パラメータの採用）を厳格に抑制します。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">相対評価の原則 (Principle of Relative Evaluation)</div>
        <div class="glossary-def">最良モデル（BIC 最小モデル）の採択は、提示された候補モデル集合（M1〜M9）の中での相対的な優位性を示すものです。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 最良モデル表示帯の注記、次点モデルとの &Delta;BIC</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 最良モデルに選定されたことは、真の独立性や因果メカニズムが成立していることを「証明」するものではありません。未測定の交絡因子や他のモデル族の可能性を排除しません。</span>
      </div>
    </div>
  </details>

  <!-- Accordion 3: ベイズ事後推論 -->
  <details class="glossary-accordion">
    <summary><span>3.</span> 多項 Dirichlet 事後推論と条件付き確率 (Bayesian Dirichlet & Conditional Inference)</summary>
    <div class="glossary-body">
      <div class="glossary-item">
        <div class="glossary-item-title">多項 Jeffreys 客観事前分布 (&alpha; = 0.5)</div>
        <div class="glossary-def">多項分布のフィッシャー情報行列の行列式に比例する客観事前分布：
```

```{r, results='asis', echo=FALSE}
cat(dirichlet_katex_display)
```

```{=html}
        ゼロ度数や低頻度セルが存在しても過度な一様平滑化を起こさず、情報幾何学的不変性を担保した事後推論を導出します。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 条件付き割合セクション（Dirichlet 推論）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> ゼロ度数セルに小さな疑似度数を付与しますが、これは数値安定化のためであり、観測が存在しない事実を歪めるものではありません。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">等裾信用区間 (Equal-Tailed Interval: 95% ETI)</div>
        <div class="glossary-def">事後確率密度の 2.5% 点から 97.5% 点までの区間。パラメータが 95% の事後確率で存在する真の範囲を示します。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 条件付き割合プロットのエラーバー（横ヒゲ）、数値表の 95%CI 列</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 頻度論の信頼区間（95% の試行で区間が真値を覆う）とは異なり、与えられたデータのもとで母数が区間内に存在する事後確率そのものを直接解釈できます。</span>
      </div>

      <div class="glossary-item">
        <div class="glossary-item-title">条件付き割合とシンプソン・パラドックス (Simpson's Paradox)</div>
        <div class="glossary-def">周辺集計における見かけの関連が、交絡変数や層別変数で条件付けた場合に消失または逆転する現象。P(Admit | Gender) の周辺割合と、P(Admit | Dept, Gender) の層別割合を対比して評価します。</div>
        <span class="glossary-meta"><strong>どこを見るか:</strong> 条件付き割合プロット（各学科 Dept ごとの男女合格率推移）</span>
        <span class="glossary-caution"><strong>誤解しないこと:</strong> 単一の周辺集計だけで「偏りがある」と結論づけてはならず、交互作用構造と層別条件付き確率を総合して解釈する必要があります。</span>
      </div>
    </div>
  </details>

  <!-- Accordion 4: 学術リファレンス -->
  <details class="glossary-accordion">
    <summary><span>4.</span> 学術リファレンス・一次文献 (Primary Academic References)</summary>
    <div class="glossary-body">
      <ol>
        <li><strong>Agresti, A. (2013).</strong> <em>Categorical Data Analysis</em> (3rd ed.). John Wiley & Sons. ［対数線形モデル、ポアソン回帰、ピアソン残差］</li>
        <li><strong>Haberman, S. J. (1973).</strong> The analysis of residuals in cross-classification tables. <em>Biometrics</em>, 29(1), 205–220. ［調整標準化残差］</li>
        <li><strong>Haberman, S. J. (1974).</strong> <em>The Analysis of Frequency Data</em>. University of Chicago Press. ［対数線形モデルの尤度理論］</li>
        <li><strong>Rao, C. R. (1948).</strong> Large sample tests of statistical hypotheses concerning several parameters with applications to problems of estimation. <em>Mathematical Proceedings of the Cambridge Philosophical Society</em>, 44(1), 50–57. ［スコア検定］</li>
        <li><strong>Schwarz, G. (1978).</strong> Estimating the dimension of a model. <em>The Annals of Statistics</em>, 6(2), 461–464. ［明示式 BIC］</li>
        <li><strong>Pregibon, D. (1981).</strong> Logistic regression diagnostics. <em>The Annals of Statistics</em>, 9(4), 705–724. ［ハット行列対角成分・レバレッジ］</li>
        <li><strong>Gelman, A., Carlin, J. B., Stern, H. S., Dunson, D. B., Vehtari, A., & Rubin, D. B. (2013).</strong> <em>Bayesian Data Analysis</em> (3rd ed.). Chapman & Hall/CRC. ［多項Dirichlet事後推論、Jeffreys事前分布］</li>
        <li><strong>Okabe, M., & Ito, K. (2008).</strong> Color Universal Design (CUD) - How to make figures and presentations that are friendly to Colorblind people. <em>J-STAGE</em>. ［学術色覚多様性パレット］</li>
      </ol>
    </div>
  </details>
</div>
```

<script>
// タブ切り替え関数
function switchCellTab(tabId) {
  document.querySelectorAll('.tab-btn').forEach(function(b) { b.classList.remove('active'); });
  document.querySelectorAll('.tab-pane').forEach(function(p) { p.classList.remove('active'); });
  var btn = document.getElementById('btn-tab-' + tabId);
  var pane = document.getElementById('pane-tab-' + tabId);
  if (btn) btn.classList.add('active');
  if (pane) pane.classList.add('active');
}

// アコーディオン展開
function openModelDetail(accordionId) {
  var details = document.getElementById(accordionId);
  if (details) {
    details.open = true;
    var heading = document.getElementById('heading-' + accordionId);
    if (heading) heading.focus(); else details.focus();
    details.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}
</script>

```{r dashboard-main-close, results='asis'}
cat('</div><!-- End .dashboard-main-content -->')
```

```{r footer, results='asis'}
cat(paste0(
  '<hr style="margin-top:40px; border-color:#e2e8f0;">',
  '<p style="font-size:11.5px; color:#94a3b8; text-align:right;">',
  "Generated by <strong>vcd-bayesian-evidence-analysis (4-Axis Multi-Baseline Canonical)</strong> &nbsp;|&nbsp; ",
  "Rao's Score Statistic &amp; Explicit BIC (N) &nbsp;|&nbsp; ",
  "完全オフライン静的HTML (KaTeX) &nbsp;|&nbsp; ",
  format(Sys.time(), "%Y-%m-%d %H:%M JST", tz = "Asia/Tokyo"),
  "</p>"
))
```
